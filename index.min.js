(function(){function r(e,n,t){function o(i,f){if(!n[i]){if(!e[i]){var c="function"==typeof require&&require;if(!f&&c)return c(i,!0);if(u)return u(i,!0);var a=new Error("Cannot find module '"+i+"'");throw a.code="MODULE_NOT_FOUND",a}var p=n[i]={exports:{}};e[i][0].call(p.exports,function(r){var n=e[i][1][r];return o(n||r)},p,p.exports,r,e,n,t)}return n[i].exports}for(var u="function"==typeof require&&require,i=0;i<t.length;i++)o(t[i]);return o}return r})()({1:[function(require,module,exports){
"use strict";

module.exports = "\n                                \u2591                                         \u25A0  \u2584\n   \u2580\u2584\u2584                  \u2584  \u25A0    \u2591  A    o  S        \u2591                  \u2584\u2584\u2588 \u2584\u2593\n    \u2590\u2588\u2588\u2588\u2584\u2584\u2584              \u2593\u2584 \u2593\u2584\u2584 \u2591                   \u2591  \u2584          \u2591\u2591\u2584\u2584\u2593\u2593\u2588\u258C\u2590\u2593\u258C\n    \u2591\u2588\u2593\u2593\u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u258C \u2590\u2593\u258C\u2590\u2593\u2588\u2588\u2593\u2584  \u2580\u2580\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2580 \u2584\u2588\u2591\u2584\u2588 \u2584\u2588\u2588\u2580\u2580 \u2584\u2584\u2593\u2590\u2593\u2593\u2588\u2588\u2588\u2580\u2588\u2588\u2588\u2588\n    \u2591\u2590\u2593\u2593\u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2593\u2588\u2580\u2588\u2588\u2588\u2588\u2588  \u2593\u2588\u2588\u2588\u2580\u2588\u2593\u2588\u2588\u2588\u2588\u2584\u2584 \u2580\u2580\u2588\u2588\u2588\u2580\u2580 \u2584\u2588\u2588\u258C\u2592\u2593\u258C\u2584  \u2584\u2584\u2593\u2593\u2593\u2593\u2588\u2590\u2588\u2580\u2580 \u2584\u2584 \u2588\u2588\u2588\u258C\n    \u2591\u2590\u2588\u2588\u2588\u2580\u2588\u2588\u258C \u2580\u2588\u2593  \u2588\u2593\u258C   \u2590\u2593\u2588\u2588   \u2591\u2580\u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2584\u2584  \u2584\u2593\u2588\u2588\u2580\u2588\u2588\u2593\u2588 \u2580\u2593\u2584 \u2580\u2593\u2593\u2593\u2588\u2588\u2590   \u258C\u2588\u2588\u2584 \u2580\u2588\u2588\u2584\n    \u2591\u2590\u2588\u2588\u2588\u2590 \u2580\u2593\u2584 \u2590\u2591  \u258C\u2592\u258C  \u2584\u2593\u2588\u2580 \u2584\u2580 \u2591  \u2580\u2593\u2593\u2584\u2588\u2588\u2580 \u2584\u2593\u2588\u2588\u2580\u2584  \u2593\u2593\u2588\u258C  \u2593\u2588 \u2590\u2593\u2588\u2588\u258C\u2590   \u258C\u2588\u2588\u2588\u258C\u2591   \u2580\n    \u2591\u2590\u2588\u2588\u2588\u2590   \u2580\u2593\u2590\u2591  \u258C\u2591 \u2584\u2588\u2580  \u2584\u2593\u258C  \u2591 \xB7 \u2590\u2593\u2588\u2588\u258C \u2593\u2593\u2588\u2588      \u2592\u2593\u2588\u2584  \u2588\u2588 \u2593\u2593\u2588\u2588\u2590\u2584  \u258C\u2588\u2588\u2588\u258C\u2591\n    \u2591\u2590\u2588\u2588\u258C\u2584\u2580   \u2590\u2593\u2591  \u258C\u2591\u2593\u2593  \u2584\u2593\u2593\u2588   \u2591    \u2593\u2588\u2588 \u2590\u2593\u2588\u2588  \u2584\u2584\u258C  \u2591 \u2591\u2591\u2580\u25A0\u2590\u2593\u258C \u2593\u2593\u2580\u2590\u2588\u2588\u2588\u2584\u2584 \u2580   \u2584\n    \u2591\u2590\u2588\u2588\u2588\u258C\u2584\u25A0  \u2593\u2590\u2591 \u2590\u258C\u2590\u2593 \u2584\u2588\u2593\u2593\u2588\u258C   \u2591\u2584\u2584\u2593\u2593\u2593\u2588\u2588 \u2593\u2593\u2588\u258C  \u2590\u2580\u2580  \u2591     \u2591\u2588\u258C\u25A0\u2580  \u2590 \u2591\u2591\u2580\u2588\u2588\u2588\u2588\u2588\u2580\n    \u2591\u2590\u2588\u2588\u2588\u2590\u258C  \u2593 \u2590\u258C \u2593\u258C\u2590\u258C\u2590\u258C\u2590\u2593\u2593\u2588\u2588\u2584\u2584\u2593\u2592\u2580\u2580 \u2590\u2592\u2588\u2588 \u2593\u2593\u2588\u258C      \u25A0\u2591     \u2588\u2588\u258C    \u2590     \u2593\u2588\u2588\u258C\n    \u2591\u2590\u2588\u2588\u2588\u2590\u2593\u2584\u2580   \u2580\u2588\u2580\u2591 \u2580\u2584\u2593 \u2593\u2593\u2588\u2588\u2580  \u2591   \u2584\u2593\u2588\u2588 \u2593\u2593\u2588\u2588\u2592\u2591 \u2584\u2584\u2580 \u2591    \u2588\u2588\u2588 \u258C \xB7 \u2590   \u258C\u2588\u2590\u2593\u2588\u2588 \u2584\n    \u2591\u2590\u2588\u2588\u2588\u2580\u2593\u258C  \u2584\u25A0 \u2590 \u2591  \u2590\u2593\u258C\u2592\u2593\u2588    \u2591   \u2590\u2593\u2588\u2588\u258C\u2590\u2593\u2593\u2588\u2588\u2593\u2593\u2588\u2580  \u2591 \u2584\u2584\u2588\u2588\u2588\u258C\u2590\u2593   \u2590   \u258C\u2588\u258C\u2593\u2588\u2588\u2588\u258C\n    \u2591\u2590\u2588\u2588\u2588\u2588\u2588\u2584\u2588\u2580   \u2590 \u2591  \u2593\u2580 \u2592\u2593\u2588\u258C   \u2591  \u2584\u2593\u2588\u258C\u2588\u2588 \u2592\u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2584\u2593\u2593\u2588  \u2590   \u258C\u2580\u2591\u2593\u2588\u2584\u2588\n    \u2591\u2590\u2588\u2588\u2588\u2588\u2588\u2580     \u2580 \u2592\u2584\u2580  \u2590\u2593\u2588\u2588\u2588\u2584  \u2591\u2584\u2593\u2593\u2588\u258C\u2584\u2588\u2588\u2588\u2584\u2580\u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2593\u2593\u2588\u2588\u2588\u2584\u2590\u2591 \u2580\u2584\u2584\u2588\u2588\u2588\u2588\u2588\n    \u2591\u2593\u2593\u2588\u2584\u2580      \u2584\u2584\u25A0\u2580  \u2584\u2593\u2593\u2588\u2584\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2590\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588 \u2588\u258C\n    \u2591\u2590\u2593\u2593\u2580    \u2584\u2593\u2593\u2580  \u2584\u2584\u2593\u2580\u2580 \u2590\u2591   \u2580\u2580\u2590\u2580\u2580\u2593\u2588\u2588\u2588\u2593\u2592\u2593\u2580\u2580\u2580\u2580\u2580\u2588\u2588\u2588\u2588\u2588\u2588\u2590\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2590\u2588\u2588\u2588\u2593\u2593\u2588\u2584   \u2580\u2584\n    \u2591\u2590\u2593   \u2584\u2593\u2588\u2588\u2588\u2584\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2593\u2593\u2584\u2584\u2584\u2584\u2584\u2590  \u2591   \u258C\u2591\u2591        \u2580\u2580\u258C \u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2580\u2588\u2588\u2590\u2588\u2588\u2584    \u2580\n    \u2591\u2590\u258C  \u2580 \u2591  \u2580\u2580\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2593\u2584\u2584\u2584\u258C\u2591\u2591          \u258C   \u2590 \u2591\u2593\u2588\u2588\u2588\u2588\u2588\u2590\u2588\u2588\u2588\u2588\u2588\u2584\n    \u2591\u2590  \u25A0  \u2591      \u2580\u2580\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u258C\u2593\u2593\u2588\u2588\u2588\u2584\u2584     \u258C  \u2584\u2593   \u2593\u258C   \u2590\u2580\u2588\u2588\u2588\u2588\u2588\u258C\n    \u2591\u2590           \u2591\u2591   \u2580\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2593\u2588\u2588\u2588\u2588\u2588\u2584\u2588\u2588\u2588\u2588\u2584\u258C\u2588\u2588\u2580    \u2590    \u2590  \u2580\u2580\u2580\u2593\u2593\u2584\n    \u2591       \u2584\u2584\u2588\u2588\u2588\u2593\u2593\u2588\u2588\u2584\u2584 \u2580\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2584\u2580\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2580   \u2584\u2584\u2590\u2584\u2584  \u2590       \u2580\u25A0\n    \u2591      \u2588\u2588\u2588\u2588\u2580 \u2591\u2591 \u2580\u2588\u2588\u2588\u2588\u2584\u2580\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2593\u2591 \u2580\u2580\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u258C \u2584\u2588\u2588\u2580\u2592\u2592 \u2580\u2588\u2588\u2590\u2584        \u25A0\n    \u2591      \u2590\u2588\u2588\u2588\u258C\u2591 \u25A0\u2592\u2592\u2590\u2588\u2588\u2588\u2588\u2593\u2590\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2593\u2591    \u2580\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2584\u2588\u2588\u2588\u2588\u258C\u2591 \u25A0\u2592\u2592\u2590\u2588\u2590\u2588\u2593\u258C\n             \u2588\u2588\u2588\u2584\u2592\u2592\u2584\u2588\u2580\u2580\u2593\u2593\u2588\u2580\u2584\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588      \u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u258C\u2580\u2588\u2588\u2588\u2588\u2584\u2591\u2591\u2591\u2584\u2588\u2588\u2590\u2588\u2593        \u25A0\n               \u2590\u2580\u2591\u2591\u2580\u2584\u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588      \u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2593 \u2580\u2580\u2588\u2588\u2593\u2593\u2588\u2588\u2588\u2588\u2590\u2580\n               \u2590    \u2580\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2593\u2593\u2588\u2588\u258C      \u2591\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2593   \u2590\u2592\u2592\u258C  \u2591\u2590         \u2591\n    \u2591 \u2591\u2591\u2591\u2592\u2592\u2592\u2593\u2593\u2588\u258C\u2588\u2588\u2588\u2588\u2588\u2584\u2588\u2588\u2588\u2588\u2588\u2580\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2591\u2591\u258C\u258C\u2580\u2584       \u2593\u2588\u2588\u2588\u2588\u2588\u2588\u258C   \u2591\u2591\u258C   \u2590         \u2593\n        \u2592\u2592\u2591   \u2590 \u2588\u2588\u2588\u2588\u2588\u2588\u2593\u2592\u2588\u2580\u2590\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2591  \u258C   \u25A0       \u2593\u2588\u2588\u2588\u2588\u2588\u2588         \u2590        \u2590\u2593\u258C\n       \u2591\u2593\u2591    \u2590 \u2590\u2588\u2588\u2588\u2588\u2593\u2591   \u2591\u2588\u2588\u2588\u2588\u2588\u2588\u2592   \u258C            \u2593\u2588\u2588\u2588\u2588\u2588\u2593        \u2590 \u2584\u25A0    \u2588\u2588\u2588\u2588\n      \u2591\u2593\u2593\u2591       \u258C\u2591      \u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2590                  \u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2591      \u2590\u2593\u2593\u2591\u2591  \u2588\u2588\u2588\u2588\u2588\n      \u2593\u2593\u2593\u2588\u2591      \u258C\u2591     \u2593\u2593\u2588\u2588\u2588\u2588\u2588                     \u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2591   \u2590\u2588\u2588\u2584\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u258C\n     \u2591\u2593\u2593\u2588\u2588\u2588\u2593     \u258C     \u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2591                \u2584     \u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2584\u2591\u2591\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u258C\n     \u2593\u2593\u2593\u2588\u2588\u2588\u2588\u2593\u2584\u2584  \u258C    \u2590\u2588\u2588\u2588\u2588\u2588\u2588\u2590         \u2591\u2591      \u2590\u2593\u2584    \u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2593\n    \u2591\u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2593\u2590\u2588\u2588\u2588\u2588\u2584\u2588\u2588\u2588\u2588\u2588\u2588\u2590      \u2584\u2584\u2593\u2593\u2584\u2584\u2584     \u2590\u2593\u2588\u2588\u2584  \u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2593\n   \u2591\u2593\u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2584\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u258C\u258C\u2590   \u2584\u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2584\u2584  \u2593\u2593\u2588\u2588\u2588\u2593 \u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\n   \u2593\u2593\u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u258C \u258C\u2590 \u2591\u2593\u2593\u258C\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2584 \u2593\u2593\u2588\u2588\u2588\u2588\u2591\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\n   \u2593\u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588   \u2590\u2591\u2593\u2593\u2580 \u2591\u2593\u2588\u2588\u2588\u2588\u2580 \u2591\u2593\u2588\u2588\u2588\u2588\u2588\u2584\u2588\u2588\u2588\u2588\u2588\u2588\u2592\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\n  \u2591\u2593\u2593\u2593\u2588\u2588\u2588\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588   \u2590\u2591\u2593\u2588\u2584  \u2591\u2593\u2588\u2588\u2588\u2584  \u2591\u2593\u2580\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u258C\n  \u2591\u2593\u2593\u2593\u2588\u2588\u2588\u2591\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u258C   \u2590\u2591\u2593\u2588\u2588\u258C\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2593\u2591\u2580\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u258C\n  \u2591\u2593\u2593\u2593\u2588\u2588\u2588\u2590\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u258C   \u2590  \u2580\u2588\u258C\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u258C  \u258C\u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\n  \u2591\u2593\u2593\u2593\u2588\u2588\u2588\u2590\u2588\u2588\u2591\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u258C\u258C   \u2590    \u258C\u2588\u258C\u2588\u2580\u2588\u2590\u2588\u2588\u2593\u2593\u2580   \u258C\u258C\u2590\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2593\n  \u2591\u2593\u2593\u2593\u2588\u2588\u2588\u2590\u2588\u2588\u2590\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588 \u258C   \u2590     \u258C \u258C \u2590          \u258C\u2591\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2593\n   \u2593\u2593\u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2590\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u258C \u258C   \u2590       \u258C \u2580          \u258C \u2591\u2590\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2593\u2588\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u258C\n   \u2591\u2593\u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2590\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588      \u2590      \u2590\u2588\u2584           \u258C   \u2593\u2588\u2588\u2588\u2588\u2588\u2593\u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\n    \u2593\u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588      \u2590\u2584\u2584\u2584\u2584\u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2584\u2584      \u258C   \u2593\u2593\u2588\u2588\u2588\u2588\u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2593\n    \u2592\u2593\u2593\u2593\u2588\u2588\u2588\u2588\u2590\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u258C   \u2584\u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2584\u258C   \u2590\u2593\u2588\u2588\u2588\u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2593\n    \u2591\u2593\u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588   \u2593\u2593\u2588\u2588\u2590\u2588\u2588\u2593\u2588\u2588\u258C\u2588\u2580\u2593\u2588\u2588\u2580\u2593\u2593\u2588\u258C\u2588\u2588\u2588\u2588\u2588\u258C  \u2590\u2593\u2588\u2588\u2588\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2591 \u2591\n     \u2591\u2593\u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588  \u2593\u2593\u2588\u2588\u2580\u2590     \u2590  \u2591\u2588\u2591     \u2580\u2580\u2588\u2588\u2593\u2588  \u2590\u2593\u2588\u2588\u2588\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2593\u2591  \u2591\n      \u2593\u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u258C  \u2590\u2588\u2588        \u2590   \u2588         \u2593\u2591\u258C\u2591 \u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2591  \u258C\u2591\n    \u2591 \u2591\u2593\u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u258C   \u258C\u258C         \u25A0\u2584\u25A0\u2590         \u2590 \u2580\u2591 \u2593\u2593\u2588\u2588\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2591  \u2590\u258C\u2591\n    \u2591  \u2591\u2593\u2593\u2588\u2588\u2588\u2588\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u258C    \u258C            \u2590         \u2590    \u2590\u2593\u2588\u2588\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2591   \u2593\u258C\u2591 \u2584\n    \u2591\u258C  \u2593\u2593\u2593\u2588\u2588\u2588\u2593\u2593\u2588\u2588\u2588\u2588\u2588     \u258C                      \u2590    \u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u258C   \u2593\u2593\u258C\u2584\u2588\n    \u2591\u2593  \u2591\u2593\u2593\u2588\u2588\u2588\u2588\u2593\u2588\u2588\u2588\u2588\u2588     \u258C                           \u2593\u2593\u2588\u2588\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2593  \u2584\u2593\u2593\u2588\u2588\u2588\n    \u2591\u2593\u258C \u2591\u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588                     \u2591           \u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2591\u2584\u2593\u2593\u2588\u2588\u2588\u2588\u258C\n \u25A0  \u2591\u2593\u2588  \u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2593\u2588\u2588\u2588\u2588\u2591    \u25A0       \u2591\u2591   \u2591\u2591             \u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2593  \u2588\u2580\u2588\u2588\u2588\u2588\u2591\n  \u2580\u2584\u2591\u2593\u2593\u258C \u2591\u2593\u2593\u2588\u2588\u2588\u2588\u2593\u2588\u2588\u2588\u2588\u2591             \u2591\u2591\u2591\u2591\u2591             \u2591\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2591  \u2590\u258C\u2588\u2588\u2588\u258C\u2591\n   \u2580\u2593\u2592\u2592\u2593  \u2591\u2593\u2593\u2588\u2588\u2588\u2588\u2593\u2588\u2588\u2588\u2592                               \u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2591   \u2590\u258C\u2588\u2588\u2588\u258C\u2591\n    \u2590\u2593\u2593\u2588\u258C  \u2593\u2593\u2588\u2588\u2588\u2588\u2593\u2588\u2588\u2588\u2588                               \u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2593\u2591     \u258C\u2588\u2588\u2588\u258C\u2591\n    \u2591\u2588\u2588\u2588\u2588  \u2591\u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u258C   \u2584\u2593\u2584                   \u25A0   \u2591\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2593       \u258C\u2588\u2588\u2588\u258C\u2591\n    \u2591\u2590\u2588\u2588\u2588\u258C  \u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u258C    \u2593\u2592\u2591                \u2584\u258C    \u2588\u2588\u2588\u2588\u2588\u2593\u2588\u2588\u2588 \u2584      \u258C\u2588\u2588\u2588\u258C\u2591\n    \u2591\u2590\u2588\u2588\u2588\u2588\u2584 \u2591\u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588    \u2590 \u2591\u2593\u2584\u2584         \u2584\u2584\u2593\u2593\u2580    \u2588\u2588\u2588\u2588\u2593\u2593\u2588\u2588\u2588\u2584\u2588       \u258C\u2588\u2588\u2588\u258C\u2591\n    \u2591\u2590\u2588\u2588\u2588\u2580\u2588\u2580 \u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588    \u2590  \u2592\u2593\u2588\u2588\u2588\u2588\u2588\u2584\u2588\u2588\u2588\u2590\u2588\u2588\u2588\u258C    \u2590\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2591       \u258C\u2588\u2588\u2588\u258C\u2591  \u25A0\n    \u2591\u2590\u2588\u2588\u2588\u2590\u258C  \u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588   \u2590   \u2590\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2580    \u2592\u2588\u2588\u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2593      \u2580\u2584\u258C\u2588\u2588\u2588\u258C\u2591\u2584\u2580\n    \u2591\u2588\u2588\u2588\u2588\u2590   \u2591\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2591 \u2590    \u2580\u2580        \u2580\u258C   \u2584\u2593\u2588\u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2593      \u25A0 \u2590\u2588\u2588\u2588\u2588\u2584\u2588\u2580\n    \u2588\u2588\u2588\u2588\u2588\u2590    \u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2590   \u2591\u2592\u2584\u2584\u2588\u2588\u2588\u2588\u2588\u2580    \u2584\u2593\u2593\u2588\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2593        \u2580\u2584\u2588\u2588\u2588\u2588\u2588\u258C\n  \u2584\u2588\u2593\u2593\u2588\u2588\u2588\u2590 \u2591 \u2591\u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2590\u2588\u2584\u2584 \u2580   \u2580\u2580   \u2584\u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u258C  \u2584       \u2590\u2588\u2593\u2593\u2588\u2588\u2591\n \u25A0\u2580 \u2591\u2590\u2588\u2588\u2588\u2590  \u2592 \u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2584\u2584\u2584\u2584\u2593\u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588 \u2584\u2593       \u2584\u2580\u2588\u2588\u2588\u2588\u2588\u258C\n    \u2591\u2590\u2588\u2588\u2588\u2590  \u2590\u2593 \u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2580\u2584\u2588\u2593       \u25A0 \u2590\u2588\u2588\u2588\u2588\u2580\u2588\u2584\n    \u2591\u2590\u2588\u2588\u2588\u2590   \u2593\u2593\u2584\u2591\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2593         \u2584\u2580\u258C\u2588\u2588\u2588\u258C\u2591\u2580\u2584\n    \u2591\u2590\u2588\u2588\u2588\u2590   \u2590\u2593\u2588\u2588\u2592\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2591           \u258C\u2588\u2588\u2588\u258C\u2591  \u25A0\n \u25A0  \u2591\u2590\u2588\u2588\u2588\u2590    \u2593\u2593\u2588\u2588\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2584\u2580\u2593\u258C            \u258C\u2588\u2588\u2588\u258C\u2591\n  \u2580\u2584\u2591\u2590\u2588\u2588\u2588\u2590    \u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2580 \u2580   \u2584\u2584\u258C      \u258C\u2588\u2588\u2588\u258C\u2591\n   \u2580\u2588\u2584\u2588\u2588\u2588\u2590  \u25A0 \u2590\u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588       \u2590\u2580\u2580      \u258C\u2588\u2588\u2588\u258C\u2591\n    \u2590\u2593\u2593\u2588\u2588\u2588\u2584\u2580  \u2590\u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588                 \u258C\u2588\u2588\u2588\u258C\u2591\n    \u2591\u2588\u2588\u2593\u2593\u2588\u258C    \u2593\u2580\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2580 \u2584             \u2584  \u258C\u2593\u2588\u2588\u258C\u2591\n    \u2590\u2588\u2588\u2588\u2588\u2588\u2580\u2584   \u2584\u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2580\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2584\u2588\u2593\u258C    Maria     \u2588\u2584\u2580\u2588\u2588\u2588\u258C\u2591\n   \u2584\u2588\u2580\u2588\u2588\u2588\u2590  \u25A0 \u2580 \u2580\u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2590\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2580 \u2590\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2591   Santissima   \u2588\u2588\u2593\u2593\u2593\u258C\u2591\n  \u2584\u2580\u2591\u2590\u2588\u2588\u2588\u2590       \u2590\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u258C \u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u258C   \u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2593   delle Grazie  \u2590\u2588\u2593\u2593\u2593\u258C\u2591\n \u25A0  \u2591\u2590\u2588\u2588\u2588\u2590     \u25A0  \u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2591 \u2590\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2591   \u2590\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588       meets       \u2588\u2588\u2588\u2588\u258C\u2591\n    \u2591\u2590\u2588\u2588\u2588\u2590      \u2588\u2584\u2590\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2591 \u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2591   \u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u258C    Frankenstein  \u2588\u2588\u2580\u2588\u2593\u258C\u2591\n    \u2591\u2590\u2588\u2588\u2588\u2590      \u2590\u2593\u258C\u2593\u2593\u2588\u2588\u2588\u2588\u2580  \u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2591     \u2593\u2593\u2588\u2588\u2588\u2588\u2588\u258C                  \u2588\u2580\u2584\u2588\u2588\u2588\u258C\u2591\n    \u2591\u2590\u2588\u2588\u2588\u2590       \u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u258C  \u2590\u2593\u2588\u2588\u2588\u2588\u2588      \u2593\u2593\u2588\u2588\u2588\u2588\u2588             \u2584\u2584\u258C  \u25A0 \u2580\u258C\u2588\u2588\u2588\u258C\u2591\n    \u2591\u2590\u2588\u2588\u2588\u2590       \u2591\u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588  \u2593\u2593\u2588\u2588\u2588\u2588      \u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588         \u25A0   \u2590\u2580\u2580     \u258C\u2588\u2588\u2588\u258C\u2591\n    \u2591\u2590\u2588\u2588\u2588\u2590        \u2590\u2593\u2588\u2588\u2588\u2588\u2588\u2593  \u2593\u2588\u2588\u2588\u2588       \u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2591                     \u258C\u2588\u2588\u2588\u258C\u2591\n    \u2591\u2590\u2588\u2588\u2588\u2590        \u2590\u2593\u2588\u2588\u2588\u2588\u2588\u258C \u2593\u2593\u2588\u2588\u2588\u2588 \u2584\u2593   \u2593\u2593\u2588\u2588\u2588\u2588\u2588\u258C      \u2584\u2584\u258C             \u258C\u2588\u2588\u2588\u258C\u2591\n    \u2591\u2590\u2588\u2588\u2593\u2590\u258C\u25A0   \u25A0   \u2593\u2593\u2588\u2588\u2588\u2588\u258C \u2590\u2593\u2588\u2588\u2588\u2584\u2588\u2588\u258C  \u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2591     \u2590\u2580\u2580       \u25A0   \u25A0\u2590\u258C\u2593\u2588\u2588\u258C\u2591\n    \u2591\u2590\u2588\u2588\u2592\u2590\u2588 \u2584\u2580     \u2593\u2593\u2588\u2588\u2588\u2588\u2591  \u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2591   \u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2591                 \u2580\u2584 \u2588\u258C\u2592\u2588\u2588\u258C\u2591\n    \u2591\u2590\u2588\u2588\u2593\u2588\u2588\u2588\u2580      \u2593\u2593\u2588\u2588\u2588\u2588\u2591 \u2590\u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2591   \u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2593                   \u2580\u2593\u2588\u2584\u2593\u2588\u2588\u258C\u2591\n    \u2591\u2590\u2588\u2588\u2588\u2588\u2588\u2588\u2580       \u2593\u2588\u2588\u2588\u2588\u2591  \u2593\u2593\u2588\u2588\u2588\u2588\u2591  \u2580 \u2590\u2593\u2588\u2588\u2580 \u2588\u258C \u2584                 \u2580\u2593\u2593\u2588\u2588\u2588\u2588\u258C\u2591\n \u25A0  \u2591\u2590\u2588\u2588\u2580\u2593\u2593\u258C        \u2593\u2588\u2588\u2588\u2588 \u2580\u2590\u2593\u2588\u2588\u2588\u2588\u2588\u2588      \u2593\u2588  \u2593\u258C       \u25A0            \u2590\u2593\u2588\u2588\u2588\u2588\u258C\u2591  \u25A0\n  \u2580\u2584\u2591\u2590\u2588\u2588\u2593\u2588\u2588\u2584\u2580      \u25A0 \u2593\u2588\u2588\u2593  \u2593\u2593\u2588\u2588\u2588\u258C\u2591\u258C      \u2590\u258C  \u2590\u258C            \u25A0      \u2580\u2584\u2593\u2580\u2588\u2588\u2588\u258C\u2591\u2584\u2580\n   \u2580\u2588\u2584\u2588\u2588\u2593\u258C\u2593\u258C  \u2584\u25A0      \u2580\u2580\u258C    \u2590\u2580\u2580  \u258C       \u258C  \u2590                 \u25A0\u2584  \u2590\u2593\u2593\u2588\u2588\u2588\u2584\u2588\u2580\n    \u2590\u2588\u2588\u2588\u2593\u2593\u2588\u2584\u2588\u2580                            \u258C                      \u2580\u2588\u2584\u2588\u2588\u2588\u2588\u2588\u2588\u258C\n     \u2588\u2588\u2588\u2588\u2588\u2588\u2580                                                       \u2580\u2593\u2593\u2588\u2588\u2588\u2588\n     \u2593\u2593\u2588\u2584\u2580                                                           \u2580\u2584\u2588\u2593\u2593\n     \u2590\u2593\u2593\u2580                                                             \u2580\u2593\u2593\u258C\n     \u2590\u2593                                                                 \u2593\u258C\n     \u2590\u258C                                                                 \u2590\u258C\n     \u2590                                                                   \u258C\n     \u25A0                                                                   \u258C\n     \u2590                                                                   \u25A0\n     \u2590                                                                   \u258C\n     \u2590\u258C                                                                 \u2590\u258C\n     \u2590\u2593                                                                 \u2593\u258C\n     \u2590\u2593\u2593\u2584                                                             \u2584\u2593\u2593\u258C\n     \u2593\u2593\u2588\u2580\u2584                                                           \u2584\u2580\u2588\u2593\u2593\n     \u2588\u2588\u2588\u2588\u2588\u2588\u2584                                                       \u2584\u2593\u2593\u2588\u2588\u2588\u2588\n    \u2590\u2588\u2588\u2580\u2588\u2588\u2588\u2580\u2588\u2584                                                   \u2584\u2588\u2580\u2588\u2593\u2593\u2588\u2588\u2588\u258C\n   \u2584\u2588\u2580\u2593\u2593\u2590\u2588\u2588\u258C  \u2580\u25A0                                               \u25A0\u2580  \u2590\u2593\u2590\u2593\u2588\u2588\u2580\u2588\u2584\n  \u2584\u2580\u2591\u2590\u2588\u2588\u2584\u2588\u2588\u2580\u2584                                                     \u2584\u2580\u2588\u2588\u2593\u2588\u2588\u258C\u2591\u2580\u2584\n \u25A0  \u2591\u2590\u2588\u2593\u2593\u2593\u2593\u258C                                                       \u2590\u2593\u2593\u2588\u2588\u2588\u258C\u2591  \u25A0\n    \u2591\u2590\u2588\u2593\u2593\u2593\u2593\u2588\u2584                                                     \u2584\u2588\u2588\u2588\u2588\u2588\u2588\u258C\u2591\n    \u2591\u2590\u2588\u2588\u2588\u2588\u2588\u2588\u2584                                                     \u2584\u2588\u2588\u2588\u2593\u2588\u2588\u258C\u2591\n    \u2591\u2590\u2588\u2588\u2592\u2590\u2588 \u2580\u2584                                                   \u2584\u2580\u2591\u2588\u258C\u2592\u2588\u2588\u258C\u2591\n    \u2591\u2590\u2588\u2588\u2593\u2590\u258C\u25A0   \u25A0                                               \u25A0   \u2591\u2590\u258C\u2593\u2588\u2588\u258C\u2591\n    \u2591\u2590\u2588\u2588\u2588\u2590                                                         \u2591 \u258C\u2588\u2588\u2588\u258C\u2591\n    \u2591\u2590\u2588\u2588\u2588\u2590                                                         \u2591 \u258C\u2588\u2588\u2588\u258C\u2591\n    \u2591\u2590\u2588\u2588\u2588\u2590                                                         \u2591 \u258C\u2588\u2588\u2588\u258C\u2591\n    \u2591\u2590\u2588\u2588\u2588\u2590                                                         \u2591 \u258C\u2588\u2588\u2588\u258C\u2591\n    \u2591\u2590\u2588\u2588\u2588\u2590                                                         \u2591 \u258C\u2588\u2588\u2588 \u2591\n \u25A0  \u2591\u2590\u2588\u2588\u2588\u2590                                                         \u2591 \u258C\u2588\u2588\u2588 \u2591\n  \u2580\u2584\u2591\u2590\u2588\u2588\u2588\u2590                                                         \u2591 \u258C\u2588\u2588\u2588 \u2591\n   \u2580\u2588\u2584\u2588\u2588\u2588\u2590  \u25A0                                                      \u2591 \u258C\u2588\u2588\u258C \u2591\n    \u2590\u2588\u2593\u2593\u2588\u2588\u2584\u2580     \u2584                                                 \u2591\u2590\u258C\u2588\u2588\u258C\u258C\u2591\n   \u25A0\u2580\u2588\u2588\u2593\u2593\u2588\u258C  \u2584\u2584\u2593\u2593\u2584\u2584\u2584\u2584\u2584\u2584\u2584\u2584\u2584\u2584\u2584\u2584\u2584\u2584\u2584\u2584\u2584\u2584\u2584\u2584\u2584\u2584\u2584\u2584\u2584\u2584\u2584\u2584\u2584\u2584\u2584\u2584\u2584\u2584\u2584\u2584\u2584\u2584\u2584\u2584\u2584\u2584  \u25A0  \u2584\u2584\u2588\u2593\u2588\u258C\u2588\u2588 \u258C\u2591\n    \u2590\u2588\u2588\u2588\u2588\u2588\u2580\u2584                                         \u2593\u2593\u2580\u2580     \u2588\u2584\u2584  \u2591 \u2584\u2588\u258C\u2590\u258C\u2591\n   \u2584\u2588\u2580\u2588\u2588\u2588\u2590  \u25A0                                       \u2580    \u2584\u2584\u258C   \u2580\u2588\u2588\u2588\u2593\u2580\u2590\u2588 \u2593\u258C\u2591\n  \u2584\u2580\u2591\u2590\u2588\u2588\u2588\u2590                                               \u2590\u2580\u2580  \u2584\u2593\u2593\u2580 \u2591\u2584\u2580\u2584\u2593\u2593\u258C\u2591\n \u25A0  \u2591\u2590\u2588\u2588\u2588\u2590                                                 \u2584\u25A0\u2580\u258C \u2584\u2584\u2580\u2580 \u2584\u2580\u2588\u2593\u2593\u2591\n    \u2591\u2590\u2588\u2588\u2588\u2590                                               \u2584\u2580\u2591\u258C \u258C\u2580   \u2584\u2588\u2588\u2588\u2588\u2588\u258C\u2591\n    \u2591\u2590\u2588\u2588\u2588\u2590                                              \u25A0  \u2591\u258C\u25A0\u258C  \u2584\u2588\u2580\u2588\u2593\u2593\u2588\u2588\u258C\u2591\n    \u2591\u2590\u2588\u2588\u2588\u2590                                             \u2590\u258C  \u2591\u258C \u258C\u25A0\u2580  \u2590\u2593\u2590\u2593\u2588\u2588\u258C\u2591\n    \u2591\u2590\u2588\u2588\u2588\u2590                                              \u2580\u2584 \u2584\u258C\u2580\u2593   \u2584\u2580\u2591 \u2593\u2588\u2588\u258C\u2591\n    \u2591\u2590\u2588\u2588\u2588\u2590                                               \u2590\u258C\u2590\u258C \u2590\u258C \u25A0  \u2591 \u2588\u2588\u2588\u258C\u2591\n    \u2591\u2590\u2588\u2588\u2588\u2590                                             \u2584\u25A0\u2580 \u2590\u258C  \u258C\u25A0    \u2590\u2588\u2588\u2588\u258C\u2591\n    \u2591\u2590\u2588\u2588\u2588\u2590                                            \u2588   \u2591\u2590   \u2593\u258C    \u2580\u2590\u2588\u2588\u258C\u2591\n    \u2592\u2593\u2588\u2588\u2588\u2590\u2584\u25A0                                         \u2590\u258C   \u2591\u2590   \u258C\u25A0\u2584    \u2588\u2588\u2588\u258C\u2591\n\u25A0\u2584 \u2584\u2593\u2588\u2588\u2584\u2588\u2580 \u2584\u2584\u2580                                        \u2593  \u2590\u2592\u2590  \u2591\u258C \u2580\u2593\u2584  \u2588\u2588\u2588\u258C\u2591\n \u2593\u2588\u2588\u2588\u2580\u2588\u2588\u258C \u2593\u2593\u2580__________________________________________\u2588\u2584\u2593\u2593\u2593\u258C \u2593\u2588\u2584 \u2590\u2588\u2588\u2584\u2588\u2588\u2588\u258C\u2591\n \u2593\u2593\u2580\u2588\u2580\u2588\u2588 \u2590\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2593\u2593\u2588\u2588\u2588\u2584\u2588\u2593\u2588\u2588\u2588\u2588\u2588\u2593\u2593\u2588\u2588\u2588\u258C\u2591\n \u2590\u258C   \u2590\u2588\u258C \u2588\u2588\u2580\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2580\u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2580\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2593\u2593\u2588\u2588\u2588\u2591\n \u2580     \u2580\u2588  \u2580\u2593                 \u2584\u2588\u2580\u2584\u2588\u2588\u2580\u2588\u2593\u2593                           \u2580\u2580\u2580\u2580\u2588\u2588\u2588\u258C\n         \u2580   \u2580    zloRfik   \u25A0\u2580 \u2584\u2588\u2580\u2580   \u2580\u2580\u2588\u2584                               \u2580\u2580\u2584\n                              \u2580           \u2580\u25A0                                \u25A0\n\n                       \u2584\u2584\u2584\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2584\u2584\n                   \u2584\u2584\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2584\u2584\n                 \u2584\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2584\n               \u2584\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2584\n              \u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2593\n             \u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2593\n            \u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2593\n           \u2588\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2593\u2593\n          \u2588\u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2593\u2588\u2593\n         \u2590\u2588\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2593\u2593\u2593\u2593\u258C\n         \u2588\u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2593\u2593\u2593\u2593\u2593\u2592\u2588\n         \u2588\u2592\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2593\u2593\u2593\u2593\u2593\u2593\u2593\u2593\u2593\u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2593\u2593\u2593\u2593\u2593\u2593\u2593\u2593\u2593\u2593\u2593\u2588\u2588\u2593\u2593\u2593\u2593\u2592\u2593\u2593\u2591\u2588\n        \u2590\u2588\u2584\u2580\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2593\u2593\u2593\u2580\u2580\u2580\u2580\u2580\u2580\u2580\u2580\u2580\u2593\u2592\u2592\u2593\u2593\u2593\u2588\u2588\u2588\u2593\u2593\u2592\u2592\u2593\u2580\u2580\u2580\u2591\u2591\u2591\u2591\u2591\u2580\u2580\u2588\u2588\u2588\u2588\u2593\u2593\u2593\u2593 \u2592\u258C\n        \u2590\u2588\u2593  \u2580\u2588\u2588\u2588\u2588\u2593\u2580\u2580\u2591          \u2591 \u2592\u2580\u2592\u2592\u2592\u2592\u2593\u2592\u2592\u2592\u2580\u2591            \u2591\u2580\u2580\u2588\u2593\u2588\u258C \u2592\u258C\n         \u2588\u2593  \u2592\u2593\u2588\u2580\u2591               \u2591 \u2591\u2590\u2592\u2592\u2591\u2591\u2592\u2592\u258C\u2591              \u2591\u2591\u2592\u2588\u2588 \u2591\u2592\u258C\n         \u2588\u2592 \u2590\u2593\u2588\u258C                  \u2591\u2592\u2580\u2592\u2593\u2588\u2588\u2593\u2593\u258C              \u2591 \u2591\u2591\u2590\u2592 \u2591\u2593\u258C\n         \u2590\u2593 \u2592\u2588\u2588                  \u2591\u2591 \u2590\u2588\u2588\u2588\u2588\u2588\u2593\u2588\u2591             \u2591\u2591\u2591\u2592\u2588\u2593 \u2591\u2593\u258C\n         \u2590\u2593\u2591 \u2593\u2588\u258C\u2591\u2591              \u2591  \u2584\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2584           \u2591\u2591\u2592\u2584\u2593\u2588\u2588\u258C \u2592\n          \u2593\u2592 \u2590\u2588\u2588\u2593\u2592\u2592\u2591          \u2591\u2592\u2593\u2584\u2593\u2588\u2588\u2588\u2588\u2580\u2591\u2580\u2591\u2588\u2593\u2593\u2588\u2584\u2584     \u2591\u2591\u2591\u2592\u2584\u2593\u2588\u2588\u2588\u2588\u2588 \u2592\n          \u2590\u2593 \u2590\u2588\u2588\u2593\u2588\u2593\u2592\u2591\u2591  \u2591\u2591\u2584\u2584\u2584\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u258C \u2591  \u2591\u2593\u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2584\u2584\u2584\u2584\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588 \u2591\n         \u2584 \u2593 \u2590\u2588\u2588\u2588\u2588\u2588\u2588\u2593\u2584\u2584\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2593\u2580\u2593\u2593\u2593\u2593\u2588\u2593      \u2592\u2593\u2592\u2592\u2593\u2593\u2593\u2588\u2588\u2593\u2580\u2580\u2593\u2588\u2593\u2593\u2588\u2588\u2588\u2588\u2588 \u2591\n             \u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2580\u2580\u2591\u2592\u2592\u2592\u2592\u2592\u2593\u2593\u2588\u2588\u2588\u2591       \u2593\u2593\u2592\u2592\u2592\u2592\u2593\u2593\u2592\u2591\u2591\u2592\u2584\u2584\u2593\u2593\u2588\u2588\u2588\u2588\u258C   \u2591\n       \u2584\u2593\u2584  \u2593\u2588\u2588\u2588\u2588\u2588\u2593\u2593\u2593\u2593\u2592\u2592\u2592\u2592\u2592\u2584\u2592\u2592\u2592\u2593\u2593\u2593\u2588\u2588\u258C   \u2591    \u2590\u2593\u2592\u2592\u2592\u2593\u2593\u2592\u2593\u2593\u2593\u2593\u2593\u2593\u2593\u2593\u2593\u2593\u2588\u2593\u2593  \u2591\u2592\n        \u2580    \u2580\u2580\u2580\u2593\u2593\u2593\u2580\u2580\u2580\u2593\u2593\u2593\u2593\u2592\u2592\u2593\u2592\u2588\u2593\u2588\u2588\u2588\u2588\u2588\u2584  \u2593\u2584\u2591 \u2584\u2588\u2588\u2593\u2592\u2592\u2593\u2593\u2592\u2593\u2588\u2593\u2592\u2580\u2580\u2580\u2593\u2593\u2593\u2580\u2580   \u2588\u2593\n     \u2580\u2593\u2588\u2588\u2588\u2593\u2593 \u2592\u2593\u2584 \u2591 \u2584\u2591\u2591  \u2580\u2580\u2593\u2592\u2588\u2593\u2588\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2584\u2584\u2593\u2588\u2588\u2588\u2588\u2593\u2592\u2593\u2588\u2588\u2593\u2593\u2592  \u2591\u2591\u2584  \u2591\u2592\u2580  \u2580\u2588\u2588\u2580\u2580\n   \u2584\u2584 \u2593\u2588 \u2584\u2588\u2584  \u2592\u2593\u2584 \u2591\u2593\u2592\u2591\u2591  \u2591 \u2593\u2593\u2588\u2593\u2588\u2588\u2588\u2593\u2588\u2588\u2593\u2588\u2588\u2588\u2588\u2580\u2588\u2593\u2588\u2588\u2588\u2593\u2592\u2593\u2588\u2592\u2588\u258C  \u2591\u2591\u2591\u2592 \u2592\u2580    \u2588\u2580 \u2592\n      \u2593\u2588       \u2580\u2593\u2584 \u2592\u2593\u2592\u2591\u2591 \u2591 \u2590\u2593\u2588\u2593\u2580\u2593\u2580\u2580\u2584\u2584\u2580\u2584\u2584\u2580\u2584\u2584\u2584\u2580\u2584\u2584\u2580\u2580\u2580\u2593\u2580\u2592\u2593\u2588 \u2591\u2591\u2592\u2592\u2593   \u2584\u2593\u2584 \u2588\u2593 \u2593\n      \u2593\u2588 \u2580        \u2580 \u2593\u2592\u2592\u2591\u2591 \u2584 \u2584\u2580\u2584\u2588\u258C\u2593\u258C\u2588\u2588\u2590\u2588\u2588\u2588\u2590\u2588\u2593\u2590\u2588\u2593\u258C\u2593\u2588\u258C\u2593\u2591\u2580\u2584 \u2591\u2592\u2593\u2593\u2593    \u2580  \u2588\u2593 \u2580\u2580\u2580 \u2580\n      \u2580\u2588    \u2580\u2588\u2588\u2588\u2588\u2593\u258C \u2593\u2593\u2592\u2592\u2591\u2591  \u2593\u258C\u2588\u2593\u2590\u2588\u2590\u2588\u2593 \u2580\u2593\u2580 \u2593\u2593\u258C\u2580\u2593\u2593\u2590\u2593\u2580\u2590\u258C\u2593 \u2591\u2592\u2593\u2593\u2593\u2588 \u2590\u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2593\u2592\u2591\n      \u2588\u2588 \u258C \u2591 \u2588\u2588 \u2584\u2584\u2584 \u2590\u2593\u2593\u2593\u2593\u2592\u2591  \u2580 \u2580 \u2584 \u2584\u2584\u2590\u2593\u2584\u2593 \u2584\u2584 \u2588\u2584\u2584 \u2584 \u2584\u2584  \u2591\u2593\u2588\u2588\u2593\u258C \u2584 \u2584\u2584\u2584 \u2588\u2588  \u2584\n      \u2588\u2588\u2591\u258C \u2593 \u2588\u2588 \u2588  \u2591\u2590\u2593\u2593\u2588\u2588\u2593\u2592\u2591 \u2584\u2588\u2590\u2588\u2593\u2590\u2588\u2593\u258C\u2593\u2593\u258C\u2590\u2593\u2593\u2590\u2593\u2593\u258C\u2593\u2588\u258C\u2593\u2590\u258C\u2591\u2593\u2588\u2588\u2588\u2588\u258C     \u2588 \u2588\u2593 \u2580\u2593\u2580\n      \u2593\u2588\u2592\u258C \u2588 \u2588\u2588 \u2593  \u2591\u2590\u2593\u2592\u2593\u2588\u2588\u2593\u2593\u2584\u2580\u2580\u2584\u2580\u2580\u2584\u2580\u2580\u2584\u2584\u2580\u2584\u2584\u2580\u2584\u2584\u2580\u2580\u2584\u2580\u2580\u2584\u2580\u2584\u2584\u2593\u2588\u2588\u2588\u2588\u2588\u2588     \u2588 \u2588\u2593\n      \u2588\u2588\u2580\u258C \u2588\u2580\u2588\u2588 \u2592   \u2590\u2593\u2592\u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2593\u2588\u2593\u2593\u2593\u2593\u2593\u2592\u2592\u2592\u2592\u2592\u2592\u2592\u2592\u2592\u2592\u2592\u2592\u2593\u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588 \u2580 \u2580\u2580\u2580 \u2588\u2588\n    \u2584 \u2588\u2588 \u258C \u2588 \u2588\u2584 \u2591    \u2593\u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2580\u2588\u2593\u2593\u2593\u2593\u2593\u2593\u2593\u2593\u2593\u2593\u2593\u2593\u2593\u2593\u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2580  \u2584\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2593\u2592\u2591\u2591 \u2591\n  \u2584 \u2584\u2584\u2593\u2588\u2584\u2584 \u2593 \u2588\u2588    \u2591 \u2590\u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2580\u2580\n           \u2592 \u2588\u2588\u2590   \u2591\u2591   \u2580\u2580\u2580\u2580\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2580\u2580  \u2584  \u2588\u2580\u2580\u2580\u2580\u2580\u2580\u2580  \u2580\n   \u2580 \u2593\u2588\u2588\u2588\u2593 \u2591 \u2588\u2588\u2590 \u2588\u2593\u2591   \u2591 \u2588\u2584    \u2584  \u2580\u2580\u2580\u2580\u2580\u2593\u2593\u2593\u2593\u2588\u2588\u2580\u2580\u2580    \u2580 \u2588\u2588  \u2593  \u2584\u2584\u2584\u2584\u2584\u2584\u2584\u2584\u2584\n \u2584\u2593\u2584 \u2588\u2588      \u2588\u2593\u2590     \u2591\u2591\u2592 \u2588\u2588 \u2584                       \u2593 \u2588\u2593  \u2592  \u2588\u2593     \u2593\u2588 \u2584\n  \u2580  \u2593\u2588      \u2588\u2593\u2590   \u2591\u2591\u2592\u2592\u2593 \u2593\u2588 \u2580\u2580\u2580 \u2580\u2580     \u2591\u2584\u2584    \u2580\u2580 \u2580\u2580\u2580\u2580 \u2588\u2593  \u2591  \u2580\u2580\u2580\u2580\u2580\u2580\u2580\u2580\u2580\u2580\u2580\u2580  \u2580\n \u2591\u2591\u2592\u2593\u2593\u2588\u2588\u2588\u2584\u2588\u2588\u2588\u2588\u2593\u2593 \u2591 \u2592\u2592\u2593\u2593\u2593 \u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2593\u2593\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2580\u2588\u2588\u2588\u2588\u2588\u2588\u2593\u2593\u2593\u2584          ol\n     \u2580\u2580          \u2593 \u2593\u2593\u2588\u2593\u2593                \u2580\u2580\n  \u2580\u2580\u2580  \u2580   \u2580 \u2580\u2580\u2580\u2580\u2580 \u2593\u2588\u2588\u2591\u2593 \u2588\u2580\u2580\u2580\u2580\u2580\u2580\u2580 \u2580\u2580           \u2580   \u2580  \u2580\u2580\u2580\u2580\u2580  \u2580\u2580\u2580\u2580\u2580\u2580\u2580\u2588\u2588\u2580\u2580\u2580\u2580\u2580\u2580\u2580\u2580";

},{}],2:[function(require,module,exports){
"use strict";

var et = require('eventthing');

module.exports = function (button) {
  var AudioContext = window.AudioContext || window.webkitAudioContext;
  var context;
  var master;
  var muted = false;
  var resuming = false;
  var failed = false;
  var voices = new Set();
  var lastPlayed = new Map();

  var updateButton = function updateButton() {
    button.textContent = failed ? 'Sound: retry' : muted ? 'Sound: off (M)' : 'Sound: on (M)';
    button.setAttribute('aria-pressed', String(!muted));
    button.title = 'Sound starts after a keypress or click. M toggles sound.';
  };

  var reportError = function reportError(error) {
    resuming = false;
    failed = true;
    console.error('Game audio could not start:', error);
    updateButton();
    button.title = 'Audio could not start. Click to retry.';
  };

  var unlock = function unlock() {
    if (!AudioContext || muted || resuming || context && context.state === 'running') return;

    try {
      if (!context) {
        context = new AudioContext();
        master = context.createGain();
        master.gain.value = 0.07;
        master.connect(context.destination);
      }

      resuming = true;
      context.resume().then(function () {
        resuming = false;
        failed = false;
        updateButton();
      })["catch"](reportError);
    } catch (error) {
      reportError(error);
    }
  };

  var tone = function tone(start, end, duration) {
    var type = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : 'triangle';
    var delay = arguments.length > 4 && arguments[4] !== undefined ? arguments[4] : 0;
    var volume = arguments.length > 5 && arguments[5] !== undefined ? arguments[5] : 0.3;
    if (!context || context.state !== 'running' || muted || voices.size >= 24) return;
    var oscillator = context.createOscillator();
    var gain = context.createGain();
    var time = context.currentTime + delay;
    oscillator.type = type;
    oscillator.frequency.setValueAtTime(start, time);
    oscillator.frequency.exponentialRampToValueAtTime(end, time + duration);
    gain.gain.setValueAtTime(0, time);
    gain.gain.linearRampToValueAtTime(volume, time + 0.008);
    gain.gain.exponentialRampToValueAtTime(0.0001, time + duration);
    oscillator.connect(gain);
    gain.connect(master);
    voices.add(oscillator);

    oscillator.onended = function () {
      oscillator.disconnect();
      gain.disconnect();
      voices["delete"](oscillator);
    };

    oscillator.start(time);
    oscillator.stop(time + duration);
  };

  var play = function play(name, interval, effect) {
    if (!context || context.state !== 'running' || muted) return;
    var now = context.currentTime;
    if (lastPlayed.has(name) && now - lastPlayed.get(name) < interval) return;
    lastPlayed.set(name, now);
    effect();
  };

  var reset = function reset() {
    voices.forEach(function (voice) {
      return voice.stop();
    });
    lastPlayed.clear();
  };

  var toggle = function toggle() {
    if (!AudioContext) return;

    if (failed) {
      unlock();
      return;
    }

    muted = !muted;
    if (master) master.gain.value = muted ? 0 : 0.07;
    if (muted) reset();else unlock();
    updateButton();
  };

  if (!AudioContext) {
    button.textContent = 'Sound unavailable';
    button.disabled = true;
    console.warn('Web Audio is not supported by this browser.');
  } else {
    updateButton();
    button.addEventListener('click', toggle);

    var gesture = function gesture(event) {
      if (event.target === button || event.code === 'KeyM' || /^[mM]$/.test(event.key || '')) return;
      unlock();
    };

    document.addEventListener('pointerdown', gesture);
    document.addEventListener('keydown', gesture);
  }

  et.on('weapon_fire', function (props) {
    if (props.kind === 'beam' || props.kind === 'rotary') return;
    play('weapon', 0.22, function () {
      if (props.kind === 'missile') tone(150, 280, 0.12, 'sine', 0, 0.12);else tone(480, 260, 0.07, 'sine', 0, 0.09);
    });
  });
  et.on('explosion', function (props) {
    if (props.size < 1) return;
    play('explosion', 0.09, function () {
      tone(90, 30, 0.22, 'triangle', 0, 0.4);
    });
  });
  et.on('upgrade', function () {
    return play('upgrade', 0.1, function () {
      var notes = [440, 660, 880];
      notes.forEach(function (frequency, i) {
        return tone(frequency, frequency * 1.1, 0.13, 'sine', i * 0.07);
      });
    });
  });
  et.on('wave', function () {
    return play('wave', 0.5, function () {
      return tone(220, 440, 0.22);
    });
  });
  et.on('ship_hit', function () {
    return play('hit', 0.15, function () {
      return tone(150, 60, 0.15, 'triangle');
    });
  });
  et.on('death', function () {
    return play('death', 0.5, function () {
      return tone(220, 40, 0.6, 'triangle');
    });
  });
  et.on('boss_arrival', function () {
    return play('boss', 0.5, function () {
      tone(110, 220, 0.5, 'triangle');
      tone(110, 220, 0.5, 'triangle', 0.6);
    });
  });
  et.on('boss_phase', function () {
    return play('phase', 0.5, function () {
      return tone(140, 420, 0.3, 'triangle');
    });
  });
  et.on('boss_shot', function () {
    return play('boss-shot', 0.5, function () {
      return tone(120, 60, 0.12, 'sine', 0, 0.12);
    });
  });
  et.on('level_complete', function () {
    return play('victory', 1, function () {
      var notes = [330, 440, 550, 660];
      notes.forEach(function (frequency, i) {
        return tone(frequency, frequency, 0.35, 'triangle', i * 0.14);
      });
    });
  });
  return {
    toggle: toggle,
    reset: reset
  };
};

},{"eventthing":19}],3:[function(require,module,exports){
"use strict";

function _objectSpread(target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i] != null ? arguments[i] : {}; var ownKeys = Object.keys(source); if (typeof Object.getOwnPropertySymbols === 'function') { ownKeys = ownKeys.concat(Object.getOwnPropertySymbols(source).filter(function (sym) { return Object.getOwnPropertyDescriptor(source, sym).enumerable; })); } ownKeys.forEach(function (key) { _defineProperty(target, key, source[key]); }); } return target; }

function _defineProperty(obj, key, value) { if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }

var Storyboard = require('./storyboard');

var et = require('eventthing');

module.exports = function (levels) {
  var round;
  var storyboard;
  var startedAt;
  var now;
  var nextRoundAt;
  var bossActive;
  var complete;

  var reset = function reset() {
    round = 0;
    storyboard = Storyboard(levels[0]);
    startedAt = 0;
    now = 0;
    nextRoundAt = null;
    bossActive = false;
    complete = false;
  };

  reset();
  return {
    reset: reset,
    getRound: function getRound() {
      return round + 1;
    },
    isTransitioning: function isTransitioning() {
      return nextRoundAt !== null;
    },
    isComplete: function isComplete() {
      return complete;
    },
    bossArrived: function bossArrived() {
      if (!complete && nextRoundAt === null) bossActive = true;
    },
    bossDefeated: function bossDefeated() {
      if (!bossActive || complete || nextRoundAt !== null) return false;
      bossActive = false;

      if (round === levels.length - 1) {
        complete = true;
        et.fire('level_complete');
      } else {
        nextRoundAt = now + 4000;
        et.fire('round_complete', {
          number: round + 1
        });
      }

      return true;
    },
    tick: function tick(ctx) {
      now = ctx.gameTime;
      if (complete) return;

      if (nextRoundAt !== null) {
        if (now < nextRoundAt) return;
        round++;
        startedAt = nextRoundAt;
        nextRoundAt = null;
        storyboard = Storyboard(levels[round]);
        et.fire('round_started', {
          number: round + 1,
          total: levels.length
        });
      }

      storyboard.tick(_objectSpread({}, ctx, {
        gameTime: now - startedAt
      }));
    }
  };
};

},{"./storyboard":13,"eventthing":19}],4:[function(require,module,exports){
"use strict";

module.exports.compose = function (ctx, layers) {
  layers.forEach(function (layer) {
    return layer.render(ctx);
  });
};

},{}],5:[function(require,module,exports){
"use strict";

module.exports = function (callback) {
  var gameTime = 0;
  var lastFrame = performance.now();
  var viewportCanvas = document.getElementById('canvas');
  var viewportContext = viewportCanvas.getContext('2d', {
    alpha: false
  });
  var bufferCanvas = document.createElement('canvas');
  var bufferContext = bufferCanvas.getContext('2d', {
    alpha: false
  });
  var background; // resize the canvas to fill browser window dynamically

  window.addEventListener('resize', resizeCanvas, false);

  function resizeCanvas() {
    viewportCanvas.width = window.innerWidth;
    viewportCanvas.height = window.innerHeight;
    bufferCanvas.width = window.innerWidth;
    bufferCanvas.height = window.innerHeight;
    background = bufferContext.createRadialGradient(window.innerWidth * 0.7, window.innerHeight * 0.25, 0, window.innerWidth * 0.5, window.innerHeight * 0.5, Math.max(window.innerWidth, window.innerHeight));
    background.addColorStop(0, '#172442');
    background.addColorStop(0.45, '#10182d');
    background.addColorStop(1, '#050914');
  }

  resizeCanvas();
  window.requestAnimationFrame(drawFrame);

  function drawFrame(now) {
    bufferContext.font = '20px Orbitron';
    bufferContext.fillStyle = background;
    bufferContext.fillRect(0, 0, viewportCanvas.width, viewportCanvas.height);
    var elapsed = Math.max(0, Math.min(50, now - lastFrame));
    gameTime += elapsed;
    callback({
      buffer: bufferContext,
      gameTime: gameTime,
      timeSinceLastFrame: elapsed
    });
    lastFrame = now; // swap the buffer for the viewport

    viewportContext.drawImage(bufferCanvas, 0, 0);
    window.requestAnimationFrame(drawFrame);
  }
};

},{}],6:[function(require,module,exports){
"use strict";

function _slicedToArray(arr, i) { return _arrayWithHoles(arr) || _iterableToArrayLimit(arr, i) || _nonIterableRest(); }

function _nonIterableRest() { throw new TypeError("Invalid attempt to destructure non-iterable instance"); }

function _iterableToArrayLimit(arr, i) { var _arr = []; var _n = true; var _d = false; var _e = undefined; try { for (var _i = arr[Symbol.iterator](), _s; !(_n = (_s = _i.next()).done); _n = true) { _arr.push(_s.value); if (i && _arr.length === i) break; } } catch (err) { _d = true; _e = err; } finally { try { if (!_n && _i["return"] != null) _i["return"](); } finally { if (_d) throw _e; } } return _arr; }

function _arrayWithHoles(arr) { if (Array.isArray(arr)) return arr; }

var _require = require('./hit_detection'),
    overlap = _require.overlap;

module.exports = function (ctx, layers, props) {
  var _props$position = _slicedToArray(props.position, 2),
      x = _props$position[0],
      y = _props$position[1],
      radius = props.radius,
      range = props.range,
      strength = props.strength,
      _props$repel = props.repel,
      repel = _props$repel === void 0 ? false : _props$repel,
      _props$absorb = props.absorb,
      absorb = _props$absorb === void 0 ? false : _props$absorb,
      enter = props.enter;

  layers.forEach(function (layer) {
    return layer.all().forEach(function (sprite) {
      if (sprite.destroyed || !sprite.accelerate || sprite.isPlayer && sprite.isDestroyed()) return;
      var extent = sprite.getExtent();

      if (overlap(extent, {
        x: x,
        y: y,
        radius: radius
      })) {
        if (enter && enter(sprite)) return;
        if (absorb && sprite.isProjectile) return sprite.removeFromLayer();
      }

      var dx = x - extent.x;
      var dy = y - extent.y;
      var distance = Math.hypot(dx, dy);
      if (distance === 0 || distance >= range) return;
      var acceleration = strength * (1 - distance / range) * (repel ? -1 : 1);
      sprite.accelerate(dx / distance * acceleration, dy / distance * acceleration, ctx.timeSinceLastFrame);
    });
  });
};

},{"./hit_detection":8}],7:[function(require,module,exports){
"use strict";

module.exports = function () {
  var velocity = [0, 0];
  var force = [0, 0];
  var drag = 6;
  return {
    accelerate: function accelerate(ax, ay) {
      force[0] += ax;
      force[1] += ay;
    },
    step: function step(dt) {
      var seconds = dt / 1000;
      var decay = Math.exp(-drag * seconds);
      var integral = (1 - decay) / drag; // Integrate acceleration and drag together to keep the pull frame-rate independent.

      return velocity.map(function (speed, axis) {
        var terminal = force[axis] / drag;
        var distance = speed * integral + terminal * (seconds - integral);
        velocity[axis] = speed * decay + terminal * (1 - decay);
        force[axis] = 0;
        return distance;
      });
    },
    reset: function reset() {
      velocity.fill(0);
      force.fill(0);
    }
  };
};

},{}],8:[function(require,module,exports){
"use strict";

module.exports.detect = function (layer1, layer2) {
  layer1.all().forEach(function (l1sprite) {
    if (l1sprite.destroyed) return;
    var extent1 = l1sprite.getExtent();
    layer2.all().forEach(function (l2sprite) {
      if (l1sprite.destroyed || l2sprite.destroyed) return;
      if (l1sprite.canHit && !l1sprite.canHit(l2sprite)) return;
      if (l2sprite.canHit && !l2sprite.canHit(l1sprite)) return;
      var extent2 = l2sprite.getExtent();

      if (overlap(extent1, extent2)) {
        l1sprite.hit(l2sprite);
        l2sprite.hit(l1sprite);
      }
    });
  });
};

function overlap(e1, e2) {
  if (e1.endX !== undefined || e2.endX !== undefined) {
    var beam = e1.endX !== undefined ? e1 : e2;
    var target = beam === e1 ? e2 : e1;
    var dx = beam.endX - beam.x;
    var dy = beam.endY - beam.y;
    var lengthSquared = dx * dx + dy * dy;
    var t = lengthSquared === 0 ? 0 : Math.max(0, Math.min(1, ((target.x - beam.x) * dx + (target.y - beam.y) * dy) / lengthSquared));
    return Math.pow(beam.radius + target.radius, 2) >= Math.pow(target.x - beam.x - t * dx, 2) + Math.pow(target.y - beam.y - t * dy, 2);
  }

  return Math.pow(e1.radius + e2.radius, 2) >= Math.pow(e2.x - e1.x, 2) + Math.pow(e2.y - e1.y, 2);
}

module.exports.overlap = overlap;

},{}],9:[function(require,module,exports){
"use strict";

var et = require('eventthing');

var keyStates = {};

module.exports.keyStates = function () {
  return keyStates;
};

module.exports.reset = function () {
  Object.keys(keyStates).forEach(function (key) {
    keyStates[key] = false;
  });
  et.fire('keychange', keyStates);
};

var keyCode = function keyCode(evt) {
  if (evt.code === 'KeyR' || /^[rR]$/.test(evt.key || '')) return 'KeyR';
  if (evt.code === 'KeyM' || /^[mM]$/.test(evt.key || '')) return 'KeyM';
  return evt.code || evt.key;
};

var subscribedKeys = new Set(['ArrowUp', 'ArrowDown', 'ArrowLeft', 'ArrowRight', 'ControlLeft', 'Space', 'KeyR', 'KeyM']);
document.addEventListener('keydown', function (evt) {
  var code = keyCode(evt);
  if (!subscribedKeys.has(code)) return;
  evt.preventDefault();
  if (evt.repeat || keyStates[code]) return;
  keyStates[code] = true;
  et.fire('keydown', code);
  et.fire('keychange', keyStates);
});
window.addEventListener('blur', module.exports.reset);
document.addEventListener('keyup', function (evt) {
  var code = keyCode(evt);
  if (!subscribedKeys.has(code)) return;
  evt.preventDefault();
  keyStates[code] = false;
  et.fire('keyup', code);
  et.fire('keychange', keyStates);
});

},{"eventthing":19}],10:[function(require,module,exports){
"use strict";

module.exports = function (config) {
  var sprites = [];
  return {
    all: function all() {
      return sprites;
    },
    clear: function clear() {
      sprites.forEach(function (sprite) {
        sprite.destroyed = true;
      });
      sprites = [];
    },
    addSprite: function addSprite(sprite) {
      sprite.removeFromLayer = function () {
        sprites = sprites.filter(function (x) {
          return x !== sprite;
        });
        sprite.destroyed = true;
      };

      sprites.push(sprite);
    },
    removeSprite: function removeSprite(sprite) {
      return sprites = sprites.filter(function (x) {
        return x !== sprite;
      });
    },
    render: function render(ctx) {
      if (config.preRender) config.preRender(ctx);
      sprites.forEach(function (sprite) {
        return sprite.render(ctx);
      });
      if (config.postRender) config.postRender(ctx);
    },
    fire: function fire(ctx, layer) {
      sprites.forEach(function (sprite) {
        (sprite.fire(ctx) || []).forEach(function (newSprite) {
          return layer.addSprite(newSprite);
        });
      });
    }
  };
};

},{}],11:[function(require,module,exports){
"use strict";

var layers = [];

module.exports.add = function (layer) {
  layers.push(layer);
  return layer;
};

module.exports.all = function () {
  return layers;
};

},{}],12:[function(require,module,exports){
"use strict";

module.exports.calculateTrajectory = function (source, target, speed) {
  var heading = Math.atan2(target[1] - source[1], target[0] - source[0]);
  return [speed * Math.cos(heading), speed * Math.sin(heading)];
};

module.exports.calculateHeading = function (source, target, speed) {
  return Math.atan2(target[1] - source[1], target[0] - source[0]);
};

var getDistanceSqrd = function getDistanceSqrd(source, target) {
  return Math.pow(target[0] - source[0], 2) + Math.pow(target[1] - source[1], 2);
};

module.exports.getNearest = function (source, targets) {
  if (targets.length === 0) return;
  var nearest = targets[0];
  var distanceSqrd = getDistanceSqrd(source, nearest.getPosition());

  for (var i = 1; i < targets.length; i++) {
    var nextDist = getDistanceSqrd(source, targets[i].getPosition());

    if (nextDist < distanceSqrd) {
      distanceSqrd = nextDist;
      nearest = targets[i];
    }
  }

  return nearest;
};

},{}],13:[function(require,module,exports){
"use strict";

module.exports = function (level) {
  var gameTime = 0;
  var actions = []; // we're passed in the time to add since the last action,
  // rather than the absolute time

  level(function (time, action) {
    gameTime += time;
    actions.push({
      action: action,
      gameTime: gameTime
    });
  });
  var lastGameTime = 0;
  return {
    tick: function tick(ctx) {
      var actionsToExecute = actions.filter(function (x) {
        return x.gameTime > lastGameTime && x.gameTime <= ctx.gameTime;
      });
      actionsToExecute.forEach(function (x) {
        x.action(ctx);
      });
      lastGameTime = ctx.gameTime;
    }
  };
};

},{}],14:[function(require,module,exports){
"use strict";

function _objectSpread(target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i] != null ? arguments[i] : {}; var ownKeys = Object.keys(source); if (typeof Object.getOwnPropertySymbols === 'function') { ownKeys = ownKeys.concat(Object.getOwnPropertySymbols(source).filter(function (sym) { return Object.getOwnPropertyDescriptor(source, sym).enumerable; })); } ownKeys.forEach(function (key) { _defineProperty(target, key, source[key]); }); } return target; }

function _defineProperty(obj, key, value) { if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }

var keyboard = require('./engine/keyboard');

var gameLoop = require('./engine/game_loop');

var compositor = require('./engine/compositor');

var layers = require('./engine/layers');

var Layer = require('./engine/layer');

var Ship = require('./sprites/ship');

var Star = require('./sprites/star');

var SimpleGun = require('./sprites/simple_gun');

var hitDetection = require('./engine/hit_detection');

var et = require('eventthing');

var Explosion = require('./sprites/explosion');

var Campaign = require('./engine/campaign');

var level1 = require('./levels/level_1');

var level2 = require('./levels/level_2');

var Upgrade = require('./sprites/upgrade');

var Text = require('./sprites/text');

var Health = require('./sprites/health_bar');

var Ascii = require('./sprites/ascii');

var Hud = require('./sprites/hud');

var Audio = require('./engine/audio');

var MuzzleFlash = require('./sprites/muzzle_flash');

var BlackHole = require('./sprites/black_hole');

var PortalPair = require('./sprites/portal_pair');

var audio = Audio(document.getElementById('sound'));
var reducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)');
var shake = 0;
var flash = 0;
var flashColour = '#65e8ff';
var starLayer = layers.add(Layer({}));
var backgroundLayer = layers.add(Layer({}));
backgroundLayer.addSprite(Ascii({}));
var hazardLayer = layers.add(Layer({}));
et.on('create_black_hole', function (props) {
  return hazardLayer.addSprite(BlackHole(props));
});
et.on('create_portal_pair', function (props) {
  return hazardLayer.addSprite(PortalPair(props));
});

for (var i = 0; i < 140; i++) {
  var z = Math.random() + 0.5;
  starLayer.addSprite(Star({
    x: Math.random() * window.innerWidth,
    y: Math.random() * window.innerHeight,
    speed: z * 50,
    colour: "rgb(".concat(80 * z, ", ").concat(Math.min(255, 160 * z), ", ").concat(Math.min(255, 210 * z), ")"),
    radius: z * 2
  }));
}

var ship = Ship({
  x: window.innerWidth * 0.5,
  y: window.innerHeight * 0.7,
  maxSpeed: 360,
  energy: 100
});
var shipLayer = Layer({});
shipLayer.addSprite(ship);
layers.add(shipLayer);
var ballisticsLayer = Layer({});
layers.add(ballisticsLayer);
var enemyBallisticsLayer = Layer({});
et.on('create_upgrade', function (props) {
  return enemyBallisticsLayer.addSprite(new Upgrade(props));
});
layers.add(enemyBallisticsLayer);
var weaponsLayer = Layer({});

var addStarterWeapon = function addStarterWeapon() {
  return weaponsLayer.addSprite(SimpleGun({
    rate: 180,
    velocity: [0, -650],
    offset: [0, -30],
    damage: 12
  }));
};

addStarterWeapon();
layers.add(weaponsLayer);
et.on('upgrade', function (upgrade) {
  if (upgrade.weapon) {
    weaponsLayer.addSprite(upgrade.weapon);
  }
});
var enemyLayer = Layer({});
layers.add(enemyLayer);
et.on('create_enemy', enemyLayer.addSprite);
var effectsLayer = Layer({});
layers.add(effectsLayer);
et.on('explosion', function (props) {
  effectsLayer.addSprite(new Explosion(props));
  if (props.size >= 1) shake = Math.min(6, Math.max(shake, props.size * 2));
});
et.on('weapon_fire', function (props) {
  return effectsLayer.addSprite(MuzzleFlash(props));
});
et.on('portal_transit', function (props) {
  return effectsLayer.addSprite(MuzzleFlash(_objectSpread({}, props, {
    kind: 'portal'
  })));
});
et.on('ship_hit', function () {
  shake = 6;
  flash = 0.16;
  flashColour = '#ff805f';
});
et.on('upgrade', function () {
  flash = 0.08;
  flashColour = '#65e8ff';
});
et.on('display_text', function (props) {
  effectsLayer.addSprite(new Text(props));
});
var hudLayer = layers.add(Layer({}));
hudLayer.addSprite(new Health({
  width: 300,
  height: 20,
  y: 20
}));
var hud = Hud();
hudLayer.addSprite(hud);
var worldLayers = layers.all().filter(function (layer) {
  return layer !== hudLayer;
});
var restartPending = false;

var requestRestart = function requestRestart() {
  restartPending = true;
};

document.getElementById('restart').addEventListener('click', requestRestart);
et.on('keydown', function (key) {
  if (key === 'KeyR') requestRestart();
  if (key === 'KeyM') audio.toggle();
});
var campaign = Campaign([level1, level2]);
var runStartedAt = 0;
var gameplayLayers = [ballisticsLayer, enemyBallisticsLayer, weaponsLayer, enemyLayer, effectsLayer, hazardLayer];
var gravityLayers = [shipLayer, enemyLayer, ballisticsLayer, enemyBallisticsLayer];
et.on('boss_arrival', campaign.bossArrived);
et.on('boss_defeated', function () {
  if (!campaign.bossDefeated()) return;
  enemyLayer.clear();
  enemyBallisticsLayer.clear();
  ballisticsLayer.clear();
  hazardLayer.clear();

  if (!campaign.isComplete()) {
    keyboard.reset();
    ship.teleport([window.innerWidth * 0.5, window.innerHeight * 0.7]);
  }
});
gameLoop(function (ctx) {
  if (restartPending) {
    restartPending = false;
    runStartedAt = ctx.gameTime;
    gameplayLayers.forEach(function (layer) {
      return layer.clear();
    });
    audio.reset();
    shake = 0;
    flash = 0;
    keyboard.reset();
    ship.recreate();
    hud.reset();
    addStarterWeapon();
    campaign.reset();
  }

  ctx.gameTime -= runStartedAt;
  ctx.ship = ship;
  ctx.enemies = enemyLayer;
  ctx.weapons = weaponsLayer;
  hazardLayer.all().forEach(function (hazard) {
    return hazard.update(ctx, gravityLayers);
  });

  if (!ship.isDestroyed() && !campaign.isComplete()) {
    campaign.tick(ctx);

    if (!campaign.isTransitioning()) {
      hitDetection.detect(ballisticsLayer, enemyLayer); // when bullets hit an enemy

      hitDetection.detect(shipLayer, enemyLayer); // when the ship hits an enemy

      hitDetection.detect(shipLayer, enemyBallisticsLayer); // when enemy bullets hit the ship

      if (!ship.isDestroyed() && !campaign.isTransitioning() && !campaign.isComplete()) {
        if (keyboard.keyStates().Space) weaponsLayer.fire(ctx, ballisticsLayer);
        enemyLayer.fire(ctx, enemyBallisticsLayer);
      }
    }
  }

  ctx.buffer.save();

  if (!reducedMotion.matches && shake > 0) {
    ctx.buffer.translate((Math.random() - 0.5) * shake, (Math.random() - 0.5) * shake);
  }

  compositor.compose(ctx, worldLayers);
  ctx.buffer.restore();

  if (!reducedMotion.matches && flash > 0) {
    ctx.buffer.save();
    ctx.buffer.globalAlpha = flash;
    ctx.buffer.fillStyle = flashColour;
    ctx.buffer.fillRect(0, 0, window.innerWidth, window.innerHeight);
    ctx.buffer.restore();
  }

  shake = Math.max(0, shake - ctx.timeSinceLastFrame * 0.02);
  flash = Math.max(0, flash - ctx.timeSinceLastFrame / 600);
  hudLayer.render(ctx);
});

},{"./engine/audio":2,"./engine/campaign":3,"./engine/compositor":4,"./engine/game_loop":5,"./engine/hit_detection":8,"./engine/keyboard":9,"./engine/layer":10,"./engine/layers":11,"./levels/level_1":15,"./levels/level_2":16,"./sprites/ascii":20,"./sprites/black_hole":23,"./sprites/explosion":28,"./sprites/health_bar":29,"./sprites/hud":31,"./sprites/muzzle_flash":33,"./sprites/portal_pair":35,"./sprites/ship":39,"./sprites/simple_gun":40,"./sprites/star":42,"./sprites/text":43,"./sprites/upgrade":44,"eventthing":19}],15:[function(require,module,exports){
"use strict";

function _objectSpread(target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i] != null ? arguments[i] : {}; var ownKeys = Object.keys(source); if (typeof Object.getOwnPropertySymbols === 'function') { ownKeys = ownKeys.concat(Object.getOwnPropertySymbols(source).filter(function (sym) { return Object.getOwnPropertyDescriptor(source, sym).enumerable; })); } ownKeys.forEach(function (key) { _defineProperty(target, key, source[key]); }); } return target; }

function _defineProperty(obj, key, value) { if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }

var et = require('eventthing');

var Spider = require('../sprites/spider_enemy');

var Boss = require('../sprites/boss_enemy');

var Drone = require('../sprites/drone_enemy');

var _require = require('./spawns'),
    createEnemy = _require.enemy,
    createUpgrade = _require.pickup,
    beetle = _require.beetle,
    createWasps = _require.wasps;

var weapons = require('./weapons');

var unlocks = {
  1: weapons.spread,
  2: weapons.twin,
  3: weapons.beam,
  5: weapons.seeker,
  7: weapons.rear
};
var support = [{
  shield: 40,
  text: '+ SHIELD'
}, {
  energy: 35,
  text: '+ REPAIR'
}, {
  speedup: 60,
  text: '+ ENGINE BOOST'
}, {
  shield: 50,
  text: '+ SHIELD'
}, {
  energy: 40,
  text: '+ REPAIR'
}, {
  shield: 50,
  text: '+ SHIELD'
}, {
  energy: 50,
  text: '+ REPAIR'
}, {
  speedup: 40,
  text: '+ ENGINE BOOST'
}, {
  shield: 60,
  text: '+ SHIELD'
}];

module.exports = function (add) {
  var _loop = function _loop(wave) {
    add(wave === 1 ? 1000 : 8000, function () {
      et.fire('wave', {
        number: wave,
        total: 9
      });

      if (wave === 1) {
        createEnemy(beetle(0.5));
      } else {
        createEnemy(beetle(0.3));
        createEnemy(beetle(0.7));
      }

      if (wave >= 4) {
        createEnemy(Spider({
          position: [wave % 2 ? 0 : window.innerWidth, 0],
          speed: 35,
          radius: 30,
          colour: '#ff789e',
          energy: 80,
          rate: 2200
        }));
      }

      if (wave >= 6) createEnemy(beetle(0.5, true));
    });
    add(2000, function () {
      if (unlocks[wave]) {
        var upgrade = unlocks[wave]();
        createUpgrade(_objectSpread({}, upgrade, {
          text: '+ ' + upgrade.name.toUpperCase()
        }));
      } else {
        createUpgrade({
          energy: 50,
          text: '+ REPAIR'
        });
      }
    });
    add(4000, function () {
      if (wave === 1) createEnemy(beetle(0.5));

      if (wave >= 2) {
        createWasps(Math.min(4, Math.floor(wave / 2)));
      }

      if (wave >= 6) createEnemy(beetle(0.2));
      if (wave === 9) createEnemy(beetle(0.8, true));
      if ([4, 7, 9].includes(wave)) createEnemy(Drone({
        position: [window.innerWidth * 0.5, -60]
      }));
      if ([5, 7, 9].includes(wave)) et.fire('create_black_hole', {
        position: [window.innerWidth * (wave === 7 ? 0.65 : 0.35), window.innerHeight * 0.4]
      });
    });
    add(4000, function () {
      return createUpgrade(support[wave - 1]);
    });
    add(4000, function () {
      createEnemy(beetle(0.2));
      createEnemy(beetle(0.8));
    });
    add(8000, function () {
      createEnemy(beetle(0.5, wave >= 7));
      if (wave >= 3) createWasps(2);
      if (wave >= 5) createEnemy(Drone({
        position: [window.innerWidth * (wave % 2 ? 0.3 : 0.7), -60]
      }));
      if (wave % 2 === 0) createUpgrade({
        energy: 25,
        text: '+ FIELD REPAIR'
      }, 0.65);
    });
  };

  for (var wave = 1; wave <= 9; wave++) {
    _loop(wave);
  }

  add(8000, function () {
    createEnemy(Boss());
    et.fire('boss_arrival', 'HIVE QUEEN');
    createUpgrade({
      energy: 50,
      shield: 50,
      text: '+ BOSS SUPPLIES'
    });
  });
};

},{"../sprites/boss_enemy":25,"../sprites/drone_enemy":27,"../sprites/spider_enemy":41,"./spawns":17,"./weapons":18,"eventthing":19}],16:[function(require,module,exports){
"use strict";

function _objectSpread(target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i] != null ? arguments[i] : {}; var ownKeys = Object.keys(source); if (typeof Object.getOwnPropertySymbols === 'function') { ownKeys = ownKeys.concat(Object.getOwnPropertySymbols(source).filter(function (sym) { return Object.getOwnPropertyDescriptor(source, sym).enumerable; })); } ownKeys.forEach(function (key) { _defineProperty(target, key, source[key]); }); } return target; }

function _defineProperty(obj, key, value) { if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }

var et = require('eventthing');

var _require = require('./spawns'),
    enemy = _require.enemy,
    pickup = _require.pickup,
    beetle = _require.beetle,
    wasps = _require.wasps;

var weapons = require('./weapons');

var Drone = require('../sprites/drone_enemy');

var RiftEnemy = require('../sprites/rift_enemy');

var Boss = require('../sprites/boss_enemy');

var unlocks = {
  1: weapons.rotary,
  2: weapons.flank,
  3: weapons.heavy,
  5: weapons.rail,
  7: weapons.nova
};

var riftEnemy = function riftEnemy(lane) {
  var carrier = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : false;
  return RiftEnemy({
    position: [window.innerWidth * lane, -60],
    carrier: carrier
  });
};

module.exports = function (add) {
  var _loop = function _loop(wave) {
    add(wave === 1 ? 1000 : 8000, function () {
      et.fire('wave', {
        number: wave,
        total: 9
      });
      enemy(beetle(0.25, true));
      enemy(beetle(0.75, wave >= 4));
      enemy(riftEnemy(0.5));
      if (wave >= 3) enemy(riftEnemy(wave % 2 ? 0.2 : 0.8, true));
    });
    add(2000, function () {
      var upgrade = unlocks[wave] ? unlocks[wave]() : {
        energy: 40,
        text: '+ REPAIR'
      };
      pickup(upgrade.weapon ? _objectSpread({}, upgrade, {
        text: '+ ' + upgrade.name.toUpperCase()
      }) : upgrade);
    });
    add(4000, function () {
      wasps(Math.min(4, 2 + Math.floor(wave / 3)));
      enemy(Drone({
        position: [window.innerWidth * 0.5, -60]
      }));
      et.fire('create_portal_pair', {
        phase: wave * 0.7
      });
    });
    add(4000, function () {
      return pickup(wave % 2 ? {
        shield: 50,
        text: '+ SHIELD'
      } : {
        energy: 45,
        text: '+ REPAIR'
      });
    });
    add(4000, function () {
      enemy(riftEnemy(0.2));
      enemy(riftEnemy(0.8));
      if (wave >= 5) enemy(beetle(0.5, true));
    });
    add(8000, function () {
      enemy(beetle(0.5, true));
      wasps(2);
      if (wave >= 6) enemy(riftEnemy(0.5, true));
      if (wave % 2 === 0) pickup({
        energy: 30,
        text: '+ FIELD REPAIR'
      }, 0.65);
    });
  };

  for (var wave = 1; wave <= 9; wave++) {
    _loop(wave);
  }

  add(8000, function () {
    enemy(Boss({
      name: 'RIFT MATRIARCH',
      maxEnergy: 4200,
      attackSpeed: 1.2,
      rift: true
    }));
    et.fire('boss_arrival', 'RIFT MATRIARCH');
    et.fire('create_portal_pair', {
      duration: 90000
    });
    pickup({
      energy: 50,
      shield: 60,
      text: '+ BOSS SUPPLIES'
    });
  });
};

},{"../sprites/boss_enemy":25,"../sprites/drone_enemy":27,"../sprites/rift_enemy":37,"./spawns":17,"./weapons":18,"eventthing":19}],17:[function(require,module,exports){
"use strict";

var et = require('eventthing');

var Beetle = require('../sprites/beetle_enemy');

var Wasp = require('../sprites/wasp_enemy');

var enemy = function enemy(sprite) {
  return et.fire('create_enemy', sprite);
};

var pickup = function pickup(upgrade) {
  var lane = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 0.5;
  return et.fire('create_upgrade', {
    position: [window.innerWidth * lane, -20],
    speed: 130,
    radius: 16,
    colour: upgrade.weapon || upgrade.shield ? '#65e8ff' : '#aaff79',
    upgrade: upgrade
  });
};

var beetle = function beetle(lane) {
  var armoured = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : false;
  return Beetle({
    position: [window.innerWidth * lane, -50],
    speed: armoured ? 35 : 55,
    radius: armoured ? 34 : 24,
    colour: armoured ? '#d994ff' : '#ff9266',
    energy: armoured ? 180 : 40,
    rate: armoured ? 2300 : 2700,
    projectileSpeed: 220,
    armoured: armoured
  });
};

var wasps = function wasps(count) {
  for (var i = 0; i < count; i++) {
    enemy(Wasp({
      position: [window.innerWidth * (i + 1) / (count + 1), -80 - i * 65],
      phase: i
    }));
  }
};

module.exports = {
  enemy: enemy,
  pickup: pickup,
  beetle: beetle,
  wasps: wasps
};

},{"../sprites/beetle_enemy":22,"../sprites/wasp_enemy":45,"eventthing":19}],18:[function(require,module,exports){
"use strict";

var SimpleGun = require('../sprites/simple_gun');

var BlasterGun = require('../sprites/blaster_gun');

var MissileLauncher = require('../sprites/missile_launcher');

var RotaryGun = require('../sprites/rotary_gun');

var NovaGun = require('../sprites/nova_gun');

module.exports = {
  spread: function spread() {
    return {
      name: 'Spread shot',
      weapon: SimpleGun({
        rate: 350,
        velocity: [0, -620],
        damage: 8,
        colour: '#65e8ff',
        radius: 6,
        barrels: [-0.32, 0, 0.32].map(function (angle) {
          return {
            angle: angle,
            offset: [0, -30]
          };
        })
      })
    };
  },
  twin: function twin() {
    return {
      name: 'Twin pulse',
      weapon: SimpleGun({
        rate: 140,
        velocity: [0, -720],
        damage: 6,
        colour: '#ffd36a',
        radius: 6,
        barrels: [-22, 22].map(function (x) {
          return {
            angle: 0,
            offset: [x, -15]
          };
        })
      })
    };
  },
  beam: function beam() {
    return {
      name: 'Beam cannon',
      weapon: BlasterGun({
        rate: 24,
        velocity: [0, -700],
        offset: [30, 0],
        damage: 2
      })
    };
  },
  seeker: function seeker() {
    return {
      name: 'Heat seeker',
      weapon: MissileLauncher({
        rate: 450,
        offset: [0, -30],
        damage: 18
      })
    };
  },
  rear: function rear() {
    return {
      name: 'Rear shot',
      weapon: SimpleGun({
        rate: 250,
        velocity: [0, 520],
        offset: [0, 30],
        damage: 12,
        colour: '#d994ff'
      })
    };
  },
  flank: function flank() {
    return {
      name: 'Flank cannons',
      weapon: SimpleGun({
        rate: 300,
        velocity: [0, -540],
        damage: 16,
        colour: '#ff9dd8',
        radius: 8,
        barrels: [-1, 1].map(function (side) {
          return {
            angle: side * Math.PI / 2,
            offset: [side * 25, 0]
          };
        })
      })
    };
  },
  heavy: function heavy() {
    return {
      name: 'Heavy pulse',
      weapon: SimpleGun({
        rate: 550,
        velocity: [0, -460],
        offset: [0, -35],
        damage: 55,
        colour: '#aaff79',
        radius: 13
      })
    };
  },
  rotary: function rotary() {
    return {
      name: 'Rotary halo',
      weapon: RotaryGun()
    };
  },
  rail: function rail() {
    return {
      name: 'Rail lance',
      weapon: SimpleGun({
        rate: 600,
        velocity: [0, -900],
        offset: [0, -32],
        damage: 40,
        colour: '#d9ffff',
        radius: 5,
        pierce: 4,
        trailLength: 65
      })
    };
  },
  nova: function nova() {
    return {
      name: 'Nova pulse',
      weapon: NovaGun()
    };
  }
};

},{"../sprites/blaster_gun":24,"../sprites/missile_launcher":32,"../sprites/nova_gun":34,"../sprites/rotary_gun":38,"../sprites/simple_gun":40}],19:[function(require,module,exports){
"use strict";

function _typeof(obj) { if (typeof Symbol === "function" && typeof Symbol.iterator === "symbol") { _typeof = function _typeof(obj) { return typeof obj; }; } else { _typeof = function _typeof(obj) { return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; }; } return _typeof(obj); }

var events = {};

var on = function on(event, func) {
  if (Array.isArray(event)) {
    // multiple events
    event.forEach(function (ev) {
      on(ev, func);
    });
    return;
  }

  if (Array.isArray(func)) {
    // multiple functions
    func.forEach(function (fn) {
      on(event, fn);
    });
    return;
  }

  if (_typeof(event) === 'object') {
    for (ev in event) {
      on(ev, event[ev]);
    }

    return;
  }

  if (!events[event]) events[event] = [];
  events[event].push(func);
};

var emit = function emit(event, arg) {
  if (Array.isArray(event)) {
    event.forEach(function (ev) {
      emit(ev, arg);
    });
    return;
  }

  (events[event] || []).forEach(function (func) {
    func(arg, event);
  });
  (events['*'] || []).forEach(function (func) {
    func(arg, event);
  });
};

var clearAll = function clearAll() {
  events = {};
};

var clear = function clear(event) {
  if (!events[event]) {
    Object.keys(events).forEach(function (key) {
      if (events[key].indexOf(event) !== -1) {
        events[key] = events[key].filter(function (x) {
          return x !== event;
        });
      }
    });
  }

  events[event] = [];
};

var methods = {
  clear: clear,
  clearAll: clearAll,
  on: on,
  sub: on,
  emit: emit,
  fire: emit,
  pub: emit
};

if (module && module.exports) {
  module.exports = methods;
} else {
  if (window) {
    window.Eventthing = methods;
  }
}

},{}],20:[function(require,module,exports){
"use strict";

var level1Art = require('../ascii/level_1').split('\n');

var width = 12 * level1Art.reduce(function (acc, value) {
  return Math.max(acc, value.length);
}, 1);
var height = 22;
var initialPosition = window.innerHeight - level1Art.length * height;

module.exports = function (props) {
  return {
    render: function render(ctx) {
      ctx.buffer.fillStyle = '#182a40';
      ctx.buffer.font = '20px Monospace';
      var y = initialPosition + ctx.gameTime / 50;
      var windowHeight = window.innerHeight;
      level1Art.forEach(function (line) {
        var changedColour = false;
        y += height;
        if (y + height <= 0) return;
        if (y - height > windowHeight) return;
        var x = (window.innerWidth - width) / 2;
        if (Math.floor(Math.random() * 1500) === 1) x += Math.random() * 100 - 50;

        if (Math.floor(Math.random() * 1500) === 1) {
          ctx.buffer.fillStyle = "rgb(".concat(Math.floor(Math.random() * 255), ", ").concat(Math.floor(Math.random() * 255), ", ").concat(Math.floor(Math.random() * 255), ")");
          changedColour = true;
        }

        ctx.buffer.fillText(line, x, y);
        if (changedColour) ctx.buffer.fillStyle = '#182a40';
      });
      ctx.buffer.font = '20px Orbitron';
    }
  };
};

},{"../ascii/level_1":1}],21:[function(require,module,exports){
"use strict";

function _toConsumableArray(arr) { return _arrayWithoutHoles(arr) || _iterableToArray(arr) || _nonIterableSpread(); }

function _nonIterableSpread() { throw new TypeError("Invalid attempt to spread non-iterable instance"); }

function _iterableToArray(iter) { if (Symbol.iterator in Object(iter) || Object.prototype.toString.call(iter) === "[object Arguments]") return Array.from(iter); }

function _arrayWithoutHoles(arr) { if (Array.isArray(arr)) { for (var i = 0, arr2 = new Array(arr.length); i < arr.length; i++) { arr2[i] = arr[i]; } return arr2; } }

function _slicedToArray(arr, i) { return _arrayWithHoles(arr) || _iterableToArrayLimit(arr, i) || _nonIterableRest(); }

function _nonIterableRest() { throw new TypeError("Invalid attempt to destructure non-iterable instance"); }

function _iterableToArrayLimit(arr, i) { var _arr = []; var _n = true; var _d = false; var _e = undefined; try { for (var _i = arr[Symbol.iterator](), _s; !(_n = (_s = _i.next()).done); _n = true) { _arr.push(_s.value); if (i && _arr.length === i) break; } } catch (err) { _d = true; _e = err; } finally { try { if (!_n && _i["return"] != null) _i["return"](); } finally { if (_d) throw _e; } } return _arr; }

function _arrayWithHoles(arr) { if (Array.isArray(arr)) return arr; }

var drawProjectile = require('./projectile');

module.exports = function (props) {
  var position = props.position,
      radius = props.radius,
      colour = props.colour,
      damage = props.damage,
      _props$age = props.age,
      age = _props$age === void 0 ? 0 : _props$age,
      createdAt = props.createdAt;
  var velocity = props.velocity.slice();
  var previous = props.previous;
  var previousVersion = previous ? previous.teleportVersion : 0;
  var firstRender = true;
  var x = position[0] + velocity[0] * age / 1000;
  var y = position[1] + velocity[1] * age / 1000;

  var endpoint = function endpoint() {
    if (previous && (previous.destroyed || previous.teleportVersion !== previousVersion)) previous = null;
    return previous ? previous.getPosition() : [x, y];
  };

  var calculatePosition = function calculatePosition(dt) {
    x += velocity[0] * dt / 1000;
    y += velocity[1] * dt / 1000;

    var _endpoint = endpoint(),
        _endpoint2 = _slicedToArray(_endpoint, 2),
        tailX = _endpoint2[0],
        tailY = _endpoint2[1];

    if (Math.min(x, tailX) - radius > window.innerWidth || Math.max(x, tailX) + radius < 0 || Math.min(y, tailY) - radius > window.innerHeight || Math.max(y, tailY) + radius < 0) instance.removeFromLayer();
  };

  var instance = {
    isProjectile: true,
    teleportVersion: 0,
    teleport: function teleport(position, direction) {
      x = position[0];
      y = position[1];
      var speed = Math.hypot.apply(Math, _toConsumableArray(velocity));
      velocity[0] = direction[0] * speed;
      velocity[1] = direction[1] * speed;
      previous = null;
      firstRender = false;
      instance.teleportVersion++;
    },
    accelerate: function accelerate(ax, ay, dt) {
      velocity[0] += ax * dt / 1000;
      velocity[1] += ay * dt / 1000;
    },
    hit: function hit(sprite) {
      return instance.removeFromLayer();
    },
    getDamage: function getDamage() {
      return damage;
    },
    getPosition: function getPosition() {
      return [x, y];
    },
    getExtent: function getExtent() {
      var _endpoint3 = endpoint(),
          _endpoint4 = _slicedToArray(_endpoint3, 2),
          endX = _endpoint4[0],
          endY = _endpoint4[1];

      return {
        x: x,
        y: y,
        radius: radius,
        endX: endX,
        endY: endY
      };
    },
    render: function render(ctx) {
      calculatePosition(firstRender && createdAt === ctx.gameTime ? 0 : ctx.timeSinceLastFrame);
      firstRender = false;
      if (instance.destroyed) return;
      drawProjectile(ctx.buffer, {
        x: x,
        y: y,
        velocity: velocity,
        radius: radius,
        colour: colour,
        end: endpoint(),
        beam: true
      });
    }
  };
  return instance;
};

},{"./projectile":36}],22:[function(require,module,exports){
"use strict";

function _slicedToArray(arr, i) { return _arrayWithHoles(arr) || _iterableToArrayLimit(arr, i) || _nonIterableRest(); }

function _nonIterableRest() { throw new TypeError("Invalid attempt to destructure non-iterable instance"); }

function _iterableToArrayLimit(arr, i) { var _arr = []; var _n = true; var _d = false; var _e = undefined; try { for (var _i = arr[Symbol.iterator](), _s; !(_n = (_s = _i.next()).done); _n = true) { _arr.push(_s.value); if (i && _arr.length === i) break; } } catch (err) { _d = true; _e = err; } finally { try { if (!_n && _i["return"] != null) _i["return"](); } finally { if (_d) throw _e; } } return _arr; }

function _arrayWithHoles(arr) { if (Array.isArray(arr)) return arr; }

var Bullet = require('./bullet');

var et = require('eventthing');

var maths = require('../engine/maths');

var GravityMotion = require('../engine/gravity_motion');

var img = new Image();
img.src = 'svg/noun_stag beetle_720017.svg';
var armouredImg = new Image();
armouredImg.src = 'svg/noun_beetle colorado potato_720020.svg';

module.exports = function (props) {
  var position = props.position,
      speed = props.speed,
      radius = props.radius,
      colour = props.colour,
      energy = props.energy,
      rate = props.rate,
      _props$armoured = props.armoured,
      armoured = _props$armoured === void 0 ? false : _props$armoured,
      _props$projectileSpee = props.projectileSpeed,
      projectileSpeed = _props$projectileSpee === void 0 ? 240 : _props$projectileSpee;
  var maxEnergy = energy;

  var _position = _slicedToArray(position, 2),
      x = _position[0],
      y = _position[1];

  var lastFired = 0;
  var lastShipPosition = [x, y];
  var gravity = GravityMotion();

  var calculatePosition = function calculatePosition(ctx) {
    lastShipPosition = ctx.ship.getPosition();
    var newHeading = maths.calculateTrajectory([x, y], lastShipPosition, speed * ctx.timeSinceLastFrame / 1000);

    var _gravity$step = gravity.step(ctx.timeSinceLastFrame),
        _gravity$step2 = _slicedToArray(_gravity$step, 2),
        pullX = _gravity$step2[0],
        pullY = _gravity$step2[1];

    x += newHeading[0] + pullX;
    y += newHeading[1] + pullY;
  };

  var instance = {
    accelerate: gravity.accelerate,
    teleport: function teleport(position) {
      x = position[0];
      y = position[1];
      gravity.reset();
    },
    hit: function hit(sprite) {
      if (instance.destroyed) return;
      energy -= sprite.getDamage();
      var explosionHeading = maths.calculateTrajectory([x, y], lastShipPosition, speed);

      if (energy <= 0) {
        et.fire('explosion', {
          position: [x, y],
          velocity: explosionHeading,
          colour: colour,
          size: 1
        });
        instance.removeFromLayer();
        et.fire('enemy_destroyed', armoured ? 250 : 100);
        return;
      }

      et.fire('explosion', {
        position: [x, y],
        velocity: explosionHeading,
        colour: colour,
        size: 0.1
      });
    },
    getDamage: function getDamage() {
      return armoured ? 30 : 15;
    },
    getPosition: function getPosition() {
      return [x, y];
    },
    getExtent: function getExtent() {
      return {
        x: x,
        y: y,
        radius: radius
      };
    },
    fire: function fire(ctx) {
      if (y < -radius) return [];
      var elapsedTime = ctx.gameTime - lastFired;

      if (elapsedTime >= rate) {
        lastFired = ctx.gameTime;
        var shipPosition = ctx.ship.getPosition();
        var heading = maths.calculateHeading([x, y], shipPosition);
        return (armoured ? [-0.25, 0, 0.25] : [0]).map(function (angle) {
          return Bullet({
            position: [x, y],
            velocity: [Math.cos(heading + angle) * projectileSpeed, Math.sin(heading + angle) * projectileSpeed],
            radius: armoured ? 8 : 6,
            colour: '#ff9266',
            damage: armoured ? 10 : 5
          });
        });
      }
    },
    render: function render(ctx) {
      calculatePosition(ctx);
      var angle = maths.calculateHeading([x, y], ctx.ship.getPosition()) + Math.PI * 0.5;
      ctx.buffer.save();
      ctx.buffer.translate(x, y);
      ctx.buffer.shadowColor = colour;
      ctx.buffer.shadowBlur = 15;
      ctx.buffer.strokeStyle = colour;
      ctx.buffer.lineWidth = 2;
      ctx.buffer.beginPath();
      ctx.buffer.arc(0, 0, radius + 5, 0, Math.PI * 2 * Math.max(0, energy) / maxEnergy);
      ctx.buffer.stroke();
      ctx.buffer.rotate(angle);
      var size = armoured ? 100 : 70;
      ctx.buffer.drawImage(armoured ? armouredImg : img, -size / 2, -size / 2, size, size);
      ctx.buffer.restore();
    }
  };
  return instance;
};

},{"../engine/gravity_motion":7,"../engine/maths":12,"./bullet":26,"eventthing":19}],23:[function(require,module,exports){
"use strict";

var applyGravity = require('../engine/gravity_field');

var drawWell = require('./well_graphic');

module.exports = function (props) {
  var position = props.position;
  var age = 0;
  var instance = {
    update: function update(ctx, affectedLayers) {
      age += ctx.timeSinceLastFrame;
      if (age >= 11000) return instance.removeFromLayer();
      if (age < 1500) return;
      applyGravity(ctx, affectedLayers, {
        position: position,
        radius: 24,
        range: 280,
        strength: 2000,
        absorb: true
      });
    },
    render: function render(ctx) {
      drawWell(ctx, {
        position: position,
        age: age,
        remaining: 11000 - age,
        label: 'GRAVITY WELL - FIGHT THE PULL'
      });
    }
  };
  return instance;
};

},{"../engine/gravity_field":6,"./well_graphic":46}],24:[function(require,module,exports){
"use strict";

function _slicedToArray(arr, i) { return _arrayWithHoles(arr) || _iterableToArrayLimit(arr, i) || _nonIterableRest(); }

function _nonIterableRest() { throw new TypeError("Invalid attempt to destructure non-iterable instance"); }

function _iterableToArrayLimit(arr, i) { var _arr = []; var _n = true; var _d = false; var _e = undefined; try { for (var _i = arr[Symbol.iterator](), _s; !(_n = (_s = _i.next()).done); _n = true) { _arr.push(_s.value); if (i && _arr.length === i) break; } } catch (err) { _d = true; _e = err; } finally { try { if (!_n && _i["return"] != null) _i["return"](); } finally { if (_d) throw _e; } } return _arr; }

function _arrayWithHoles(arr) { if (Array.isArray(arr)) return arr; }

var Beam = require('./beam');

var et = require('eventthing');

var img = new Image();
img.src = 'svg/gattling-gun.svg';

module.exports = function (props) {
  var rate = props.rate,
      velocity = props.velocity,
      offset = props.offset,
      damage = props.damage;

  var _offset = _slicedToArray(offset, 2),
      dx = _offset[0],
      dy = _offset[1];

  var previous = null;
  var lastSegment = null;
  var lastSegmentVersion = 0;
  var shipVersion = 0;
  var nextShot = 0;
  var heat = 0;
  var overheated = false;
  var lastUpdate = null;

  var cool = function cool(ctx) {
    var firing = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : false;

    if (lastUpdate !== null && (overheated || !firing)) {
      heat = Math.max(0, heat - (ctx.gameTime - lastUpdate) * 0.055);
    }

    if (overheated && heat <= 20) overheated = false;
    lastUpdate = ctx.gameTime;
  };

  return {
    getHeat: function getHeat() {
      return heat;
    },
    isOverheated: function isOverheated() {
      return overheated;
    },
    fire: function fire(ctx) {
      cool(ctx, true);

      if (overheated) {
        previous = null;
        lastSegment = null;
        return [];
      }

      var position = ctx.ship.getPosition();
      var now = ctx.gameTime;
      var currentShipVersion = ctx.ship.teleportVersion || 0;

      if (!previous || now - previous.time > ctx.timeSinceLastFrame + 0.01 || currentShipVersion !== shipVersion) {
        previous = {
          position: position,
          time: now
        };
        lastSegment = null;
        nextShot = now;
      }

      shipVersion = currentShipVersion;
      if (lastSegment && lastSegment.teleportVersion !== lastSegmentVersion) lastSegment = null;
      var shots = [];

      while (nextShot <= now) {
        var elapsed = now - previous.time;
        var t = elapsed > 0 ? (nextShot - previous.time) / elapsed : 1;
        var x = previous.position[0] + (position[0] - previous.position[0]) * t;
        var y = previous.position[1] + (position[1] - previous.position[1]) * t;
        var segment = Beam({
          position: [x + dx + 5, y + dy - 50],
          velocity: velocity,
          colour: '#d994ff',
          damage: damage,
          radius: 6,
          age: now - nextShot,
          createdAt: now,
          previous: lastSegment
        });
        shots.push(segment);
        lastSegment = segment;
        lastSegmentVersion = segment.teleportVersion;
        nextShot += rate;
        heat = Math.min(100, heat + 2);

        if (heat === 100) {
          overheated = true;
          break;
        }
      }

      previous = {
        position: position,
        time: now
      };
      if (shots.length) et.fire('weapon_fire', {
        kind: 'beam',
        colour: '#d994ff',
        position: [position[0] + dx + 5, position[1] + dy - 50]
      });
      return shots;
    },
    render: function render(ctx) {
      cool(ctx);
      if (ctx.ship.isDestroyed()) return;

      var _ctx$ship$getPosition = ctx.ship.getPosition(),
          _ctx$ship$getPosition2 = _slicedToArray(_ctx$ship$getPosition, 2),
          x = _ctx$ship$getPosition2[0],
          y = _ctx$ship$getPosition2[1];

      ctx.buffer.translate(x, y);
      ctx.buffer.rotate(-Math.PI / 2);
      ctx.buffer.drawImage(img, -20, 10, 50, 50);
      ctx.buffer.rotate(Math.PI / 2);
      ctx.buffer.translate(-x, -y);
    }
  };
};

},{"./beam":21,"eventthing":19}],25:[function(require,module,exports){
"use strict";

function _slicedToArray(arr, i) { return _arrayWithHoles(arr) || _iterableToArrayLimit(arr, i) || _nonIterableRest(); }

function _nonIterableRest() { throw new TypeError("Invalid attempt to destructure non-iterable instance"); }

function _iterableToArrayLimit(arr, i) { var _arr = []; var _n = true; var _d = false; var _e = undefined; try { for (var _i = arr[Symbol.iterator](), _s; !(_n = (_s = _i.next()).done); _n = true) { _arr.push(_s.value); if (i && _arr.length === i) break; } } catch (err) { _d = true; _e = err; } finally { try { if (!_n && _i["return"] != null) _i["return"](); } finally { if (_d) throw _e; } } return _arr; }

function _arrayWithHoles(arr) { if (Array.isArray(arr)) return arr; }

var et = require('eventthing');

var Bullet = require('./bullet');

var maths = require('../engine/maths');

var GravityMotion = require('../engine/gravity_motion');

var img = new Image();
img.src = 'svg/hive_queen.svg';

module.exports = function () {
  var props = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {};
  var _props$name = props.name,
      name = _props$name === void 0 ? 'HIVE QUEEN' : _props$name,
      _props$maxEnergy = props.maxEnergy,
      maxEnergy = _props$maxEnergy === void 0 ? 2600 : _props$maxEnergy,
      _props$attackSpeed = props.attackSpeed,
      attackSpeed = _props$attackSpeed === void 0 ? 1 : _props$attackSpeed,
      _props$rift = props.rift,
      rift = _props$rift === void 0 ? false : _props$rift;
  var energy = maxEnergy;
  var x = window.innerWidth / 2;
  var y = -120;
  var age = 0;
  var lastFired = 0;
  var volleys = 0;
  var phase = 1;
  var gravity = GravityMotion();
  var driftX = 0;
  var driftY = 0;
  var pathX = x;
  var pathY = y;

  var colour = function colour() {
    return (rift ? ['#a8ffff', '#b5a0ff', '#ffffff'] : ['#d994ff', '#ff9dd8', '#ff805f'])[phase - 1];
  };

  var instance = {
    isBoss: true,
    accelerate: gravity.accelerate,
    teleport: function teleport(position) {
      x = position[0];
      y = position[1];
      driftX = x - pathX;
      driftY = y - pathY;
      gravity.reset();
    },
    getName: function getName() {
      return name;
    },
    getPosition: function getPosition() {
      return [x, y];
    },
    getExtent: function getExtent() {
      return {
        x: x,
        y: y,
        radius: 82
      };
    },
    getHealth: function getHealth() {
      return energy;
    },
    getMaxHealth: function getMaxHealth() {
      return maxEnergy;
    },
    getPhase: function getPhase() {
      return phase;
    },
    getDamage: function getDamage() {
      return 25;
    },
    hit: function hit(sprite) {
      if (instance.destroyed || sprite.isPlayer) return;
      energy = Math.max(0, energy - sprite.getDamage());

      if (energy === 0) {
        instance.removeFromLayer();

        for (var _i = 0, _arr = [-65, 0, 65]; _i < _arr.length; _i++) {
          var offset = _arr[_i];
          et.fire('explosion', {
            position: [x + offset, y],
            velocity: [offset, 20],
            colour: colour(),
            size: offset === 0 ? 5 : 2
          });
        }

        et.fire('enemy_destroyed', rift ? 4000 : 2500);
        et.fire('boss_defeated');
        return;
      }

      var nextPhase = energy > maxEnergy * 2 / 3 ? 1 : energy > maxEnergy / 3 ? 2 : 3;

      if (phase !== nextPhase) {
        phase = nextPhase;
        et.fire('boss_phase', phase);
        et.fire('explosion', {
          position: [x, y],
          velocity: [0, 0],
          colour: colour(),
          size: 1
        });
      }
    },
    fire: function fire(ctx) {
      if (age < 3000 || ctx.gameTime - lastFired < [1800, 1350, 950][phase - 1] / attackSpeed) return [];
      lastFired = ctx.gameTime;
      volleys++;
      var heading = maths.calculateHeading([x, y], ctx.ship.getPosition());

      var shot = function shot(angle) {
        return Bullet({
          position: [x, y + 45],
          velocity: [Math.cos(angle) * (160 + phase * 25), Math.sin(angle) * (160 + phase * 25)],
          radius: 6,
          colour: '#ff805f',
          damage: 7
        });
      };

      var shots = [];

      for (var i = -phase; i <= phase; i++) {
        shots.push(shot(heading + i * 0.16));
      }

      if (phase > 1 && volleys % 3 === 0) {
        var count = phase === 3 ? 16 : 12;

        for (var _i2 = 0; _i2 < count; _i2++) {
          shots.push(shot(_i2 * Math.PI * 2 / count + age / 1600));
        }
      }

      et.fire('boss_shot');
      return shots;
    },
    render: function render(ctx) {
      age += ctx.timeSinceLastFrame;

      var _gravity$step = gravity.step(ctx.timeSinceLastFrame),
          _gravity$step2 = _slicedToArray(_gravity$step, 2),
          pullX = _gravity$step2[0],
          pullY = _gravity$step2[1];

      driftX += pullX;
      driftY += pullY;
      var entry = Math.min(1, age / 3000);
      pathX = window.innerWidth / 2 + Math.sin(age / 1600) * Math.min(280, window.innerWidth * 0.3) * entry;
      pathY = -120 + (Math.min(160, window.innerHeight * 0.24) + 120) * entry + Math.sin(age / 800) * 12 * entry;
      x = Math.max(82, Math.min(window.innerWidth - 82, pathX + driftX));
      y = pathY + driftY;
      if (entry === 1) y = Math.max(82, Math.min(window.innerHeight - 82, y));
      var buffer = ctx.buffer;
      buffer.save();
      buffer.translate(x, y);
      buffer.shadowColor = colour();
      buffer.shadowBlur = 24;
      buffer.strokeStyle = colour();
      buffer.fillStyle = '#241637';
      buffer.lineWidth = 3;

      for (var _i3 = 0, _arr2 = [-1, 1]; _i3 < _arr2.length; _i3++) {
        var side = _arr2[_i3];
        buffer.beginPath();
        buffer.moveTo(side * 40, -45);
        buffer.lineTo(side * 125, -65 + Math.sin(age / 140) * 12);
        buffer.lineTo(side * 105, 30);
        buffer.lineTo(side * 45, 55);
        buffer.closePath();
        buffer.fill();
        buffer.stroke();
        buffer.beginPath();
        buffer.moveTo(side * 45, 0);
        buffer.lineTo(side * 110, -35);
        buffer.stroke();
      }

      buffer.save();
      if (rift) buffer.filter = 'hue-rotate(65deg)';
      buffer.drawImage(img, -130, -120, 260, 240);
      buffer.restore();
      buffer.shadowBlur = 12;
      buffer.rotate(age / 1200);

      for (var i = 0; i < 6; i++) {
        var angle = i * Math.PI / 3;
        buffer.beginPath();
        buffer.arc(Math.cos(angle) * 86, Math.sin(angle) * 86, 4, 0, Math.PI * 2);
        buffer.fillStyle = colour();
        buffer.fill();
      }

      buffer.restore();
    }
  };
  return instance;
};

},{"../engine/gravity_motion":7,"../engine/maths":12,"./bullet":26,"eventthing":19}],26:[function(require,module,exports){
"use strict";

function _slicedToArray(arr, i) { return _arrayWithHoles(arr) || _iterableToArrayLimit(arr, i) || _nonIterableRest(); }

function _nonIterableRest() { throw new TypeError("Invalid attempt to destructure non-iterable instance"); }

function _iterableToArrayLimit(arr, i) { var _arr = []; var _n = true; var _d = false; var _e = undefined; try { for (var _i = arr[Symbol.iterator](), _s; !(_n = (_s = _i.next()).done); _n = true) { _arr.push(_s.value); if (i && _arr.length === i) break; } } catch (err) { _d = true; _e = err; } finally { try { if (!_n && _i["return"] != null) _i["return"](); } finally { if (_d) throw _e; } } return _arr; }

function _arrayWithHoles(arr) { if (Array.isArray(arr)) return arr; }

var drawProjectile = require('./projectile');

module.exports = function (props) {
  var position = props.position,
      velocity = props.velocity,
      radius = props.radius,
      colour = props.colour,
      damage = props.damage,
      _props$pierce = props.pierce,
      pierce = _props$pierce === void 0 ? 1 : _props$pierce,
      trailLength = props.trailLength;
  var hitTargets = new Set();

  var _position = _slicedToArray(position, 2),
      x = _position[0],
      y = _position[1];

  var _velocity = _slicedToArray(velocity, 2),
      dx = _velocity[0],
      dy = _velocity[1];

  var calculatePosition = function calculatePosition(dt) {
    x += dx * dt / 1000;
    y += dy * dt / 1000;
    if (x > window.innerWidth + 60 || x < -60 || y > window.innerHeight + 60 || y < -60) instance.removeFromLayer();
  };

  var instance = {
    isProjectile: true,
    teleport: function teleport(position, direction) {
      x = position[0];
      y = position[1];
      var speed = Math.hypot(dx, dy);
      dx = direction[0] * speed;
      dy = direction[1] * speed;
    },
    accelerate: function accelerate(ax, ay, dt) {
      dx += ax * dt / 1000;
      dy += ay * dt / 1000;
    },
    canHit: function canHit(sprite) {
      return !hitTargets.has(sprite);
    },
    hit: function hit(sprite) {
      hitTargets.add(sprite);
      pierce--;
      if (pierce <= 0) instance.removeFromLayer();
    },
    getDamage: function getDamage() {
      return damage;
    },
    getExtent: function getExtent() {
      return {
        x: x,
        y: y,
        radius: radius
      };
    },
    render: function render(ctx) {
      calculatePosition(ctx.timeSinceLastFrame);
      drawProjectile(ctx.buffer, {
        x: x,
        y: y,
        velocity: [dx, dy],
        radius: radius,
        colour: colour,
        length: trailLength || Math.max(22, radius * 5)
      });
    }
  };
  return instance;
};

},{"./projectile":36}],27:[function(require,module,exports){
"use strict";

function _slicedToArray(arr, i) { return _arrayWithHoles(arr) || _iterableToArrayLimit(arr, i) || _nonIterableRest(); }

function _nonIterableRest() { throw new TypeError("Invalid attempt to destructure non-iterable instance"); }

function _iterableToArrayLimit(arr, i) { var _arr = []; var _n = true; var _d = false; var _e = undefined; try { for (var _i = arr[Symbol.iterator](), _s; !(_n = (_s = _i.next()).done); _n = true) { _arr.push(_s.value); if (i && _arr.length === i) break; } } catch (err) { _d = true; _e = err; } finally { try { if (!_n && _i["return"] != null) _i["return"](); } finally { if (_d) throw _e; } } return _arr; }

function _arrayWithHoles(arr) { if (Array.isArray(arr)) return arr; }

var et = require('eventthing');

var Bullet = require('./bullet');

var GravityMotion = require('../engine/gravity_motion');

module.exports = function (props) {
  var _props$position = _slicedToArray(props.position, 2),
      startX = _props$position[0],
      startY = _props$position[1];

  var x = startX;
  var y = startY;
  var age = 0;
  var lastFired = 0;
  var energy = 65;
  var gravity = GravityMotion();
  var driftX = 0;
  var driftY = 0;
  var pathX = x;
  var pathY = y;
  var instance = {
    accelerate: gravity.accelerate,
    teleport: function teleport(position) {
      x = position[0];
      y = position[1];
      driftX = x - pathX;
      driftY = y - pathY;
      gravity.reset();
    },
    getPosition: function getPosition() {
      return [x, y];
    },
    getExtent: function getExtent() {
      return {
        x: x,
        y: y,
        radius: 25
      };
    },
    getDamage: function getDamage() {
      return 18;
    },
    hit: function hit(sprite) {
      if (instance.destroyed) return;
      energy -= sprite.getDamage();
      et.fire('explosion', {
        position: [x, y],
        velocity: [0, 35],
        colour: '#ffbc59',
        size: energy <= 0 ? 1 : 0.1
      });

      if (energy <= 0) {
        instance.removeFromLayer();
        et.fire('enemy_destroyed', 225);
      }
    },
    fire: function fire(ctx) {
      if (age < 1500 || y < 0 || ctx.gameTime - lastFired < 2100) return [];
      lastFired = ctx.gameTime;
      return Array.from({
        length: 6
      }, function (_, i) {
        var angle = age / 800 + i * Math.PI / 3;
        return Bullet({
          position: [x, y],
          velocity: [Math.cos(angle) * 190, Math.sin(angle) * 190],
          radius: 5,
          colour: '#ffbc59',
          damage: 5
        });
      });
    },
    render: function render(ctx) {
      age += ctx.timeSinceLastFrame;

      var _gravity$step = gravity.step(ctx.timeSinceLastFrame),
          _gravity$step2 = _slicedToArray(_gravity$step, 2),
          pullX = _gravity$step2[0],
          pullY = _gravity$step2[1];

      driftX += pullX;
      driftY += pullY;
      pathX = startX + Math.sin(age / 800) * 110;
      pathY = startY + age * 0.045;
      x = Math.max(35, Math.min(window.innerWidth - 35, pathX + driftX));
      y = pathY + driftY;
      if (y > window.innerHeight + 60) return instance.removeFromLayer();
      var buffer = ctx.buffer;
      buffer.save();
      buffer.translate(x, y);
      buffer.rotate(age / 700);
      buffer.shadowColor = '#ffbc59';
      buffer.shadowBlur = 14;
      buffer.strokeStyle = '#ffbc59';
      buffer.fillStyle = '#402b32';
      buffer.lineWidth = 2;

      for (var i = 0; i < 3; i++) {
        buffer.rotate(Math.PI * 2 / 3);
        buffer.beginPath();
        buffer.moveTo(8, -8);
        buffer.lineTo(34, -20);
        buffer.lineTo(26, 16);
        buffer.lineTo(8, 8);
        buffer.closePath();
        buffer.fill();
        buffer.stroke();
      }

      buffer.fillStyle = '#fff0ba';
      buffer.beginPath();
      buffer.arc(0, 0, 9, 0, Math.PI * 2);
      buffer.fill();
      buffer.restore();
    }
  };
  return instance;
};

},{"../engine/gravity_motion":7,"./bullet":26,"eventthing":19}],28:[function(require,module,exports){
"use strict";

function _slicedToArray(arr, i) { return _arrayWithHoles(arr) || _iterableToArrayLimit(arr, i) || _nonIterableRest(); }

function _nonIterableRest() { throw new TypeError("Invalid attempt to destructure non-iterable instance"); }

function _iterableToArrayLimit(arr, i) { var _arr = []; var _n = true; var _d = false; var _e = undefined; try { for (var _i = arr[Symbol.iterator](), _s; !(_n = (_s = _i.next()).done); _n = true) { _arr.push(_s.value); if (i && _arr.length === i) break; } } catch (err) { _d = true; _e = err; } finally { try { if (!_n && _i["return"] != null) _i["return"](); } finally { if (_d) throw _e; } } return _arr; }

function _arrayWithHoles(arr) { if (Array.isArray(arr)) return arr; }

var twopi = 2 * Math.PI;

var Spec = function Spec(props) {
  var position = props.position,
      velocity = props.velocity,
      colour = props.colour;

  var calculatePosition = function calculatePosition(dt) {
    position[0] += velocity[0] * dt / 1000;
    position[1] += velocity[1] * dt / 1000;
  };

  return {
    render: function render(ctx, life) {
      calculatePosition(ctx.timeSinceLastFrame);
      ctx.buffer.fillStyle = colour;
      ctx.buffer.strokeStyle = colour;
      ctx.buffer.lineWidth = 2;
      ctx.buffer.beginPath();
      ctx.buffer.moveTo(position[0], position[1]);
      ctx.buffer.lineTo(position[0] - velocity[0] * 0.045, position[1] - velocity[1] * 0.045);
      ctx.buffer.stroke();
      ctx.buffer.beginPath();
      ctx.buffer.arc(position[0], position[1], Math.max(0.5, (500 - life) / 160), 0, twopi);
      ctx.buffer.fill();
    }
  };
};

module.exports = function (props) {
  var position = props.position,
      velocity = props.velocity,
      colour = props.colour,
      size = props.size;

  var _position = _slicedToArray(position, 2),
      x = _position[0],
      y = _position[1];

  var _velocity = _slicedToArray(velocity, 2),
      dx = _velocity[0],
      dy = _velocity[1];

  var life = 0;
  var childSprites = [];
  var specSpeed = 480;
  var specCount = 24;

  for (var i = 0; i < specCount * size; i++) {
    childSprites.push(Spec({
      colour: colour,
      position: [x, y],
      velocity: [velocity[0] + specSpeed * (Math.random() - 0.5), velocity[1] + specSpeed * (Math.random() - 0.5)]
    }));
  }

  var calculatePosition = function calculatePosition(dt) {
    x += velocity[0] * dt / 1000;
    y += velocity[1] * dt / 1000;
    if (dx > 0 && x > window.innerWidth) return instance.removeFromLayer();
    if (dx < 0 && x < 0) return instance.removeFromLayer();
    if (dy > 0 && y > window.innerHeight) return instance.removeFromLayer();
    if (dy < 0 && y < 0) return instance.removeFromLayer();
  };

  var instance = {
    render: function render(ctx) {
      calculatePosition(ctx.timeSinceLastFrame);
      ctx.buffer.save();
      ctx.buffer.globalCompositeOperation = 'lighter';
      ctx.buffer.globalAlpha = Math.max(0, 1 - life / 500);
      ctx.buffer.shadowColor = colour;
      ctx.buffer.shadowBlur = 12;

      if (size >= 1) {
        ctx.buffer.strokeStyle = '#ffffff';
        ctx.buffer.lineWidth = 2;
        ctx.buffer.beginPath();
        ctx.buffer.arc(x, y, life / 5, 0, twopi);
        ctx.buffer.stroke();
        ctx.buffer.strokeStyle = colour;
        ctx.buffer.lineWidth = 4;
        ctx.buffer.beginPath();
        ctx.buffer.arc(x, y, life / 8, 0, twopi);
        ctx.buffer.stroke();
      }

      childSprites.forEach(function (x) {
        return x.render(ctx, life);
      });
      ctx.buffer.restore();
      life += ctx.timeSinceLastFrame;
      if (life > 500) instance.removeFromLayer();
    }
  };
  return instance;
};

},{}],29:[function(require,module,exports){
"use strict";

var colour = 'rgb(152, 195, 121)';

module.exports = function (props) {
  var width = props.width,
      height = props.height,
      y = props.y;
  return {
    render: function render(ctx) {
      var health = ctx.ship.getHealth();
      var x = (window.innerWidth - width) / 2;
      ctx.buffer.beginPath();
      ctx.buffer.lineWidth = 1;
      ctx.buffer.strokeStyle = colour;
      ctx.buffer.rect(x - 4, y - 4, width + 4, height + 4);
      ctx.buffer.stroke();
      ctx.buffer.beginPath();
      ctx.buffer.fillStyle = colour;
      ctx.buffer.rect(x - 2, y - 2, width * health / 100, height);
      ctx.buffer.fill();
    }
  };
};

},{}],30:[function(require,module,exports){
"use strict";

function _toConsumableArray(arr) { return _arrayWithoutHoles(arr) || _iterableToArray(arr) || _nonIterableSpread(); }

function _nonIterableSpread() { throw new TypeError("Invalid attempt to spread non-iterable instance"); }

function _iterableToArray(iter) { if (Symbol.iterator in Object(iter) || Object.prototype.toString.call(iter) === "[object Arguments]") return Array.from(iter); }

function _arrayWithoutHoles(arr) { if (Array.isArray(arr)) { for (var i = 0, arr2 = new Array(arr.length); i < arr.length; i++) { arr2[i] = arr[i]; } return arr2; } }

function _slicedToArray(arr, i) { return _arrayWithHoles(arr) || _iterableToArrayLimit(arr, i) || _nonIterableRest(); }

function _nonIterableRest() { throw new TypeError("Invalid attempt to destructure non-iterable instance"); }

function _iterableToArrayLimit(arr, i) { var _arr = []; var _n = true; var _d = false; var _e = undefined; try { for (var _i = arr[Symbol.iterator](), _s; !(_n = (_s = _i.next()).done); _n = true) { _arr.push(_s.value); if (i && _arr.length === i) break; } } catch (err) { _d = true; _e = err; } finally { try { if (!_n && _i["return"] != null) _i["return"](); } finally { if (_d) throw _e; } } return _arr; }

function _arrayWithHoles(arr) { if (Array.isArray(arr)) return arr; }

var drawProjectile = require('./projectile');

var speed = 480;

var maths = require('../engine/maths');

module.exports = function (props) {
  var position = props.position,
      radius = props.radius,
      damage = props.damage;

  var _position = _slicedToArray(position, 2),
      x = _position[0],
      y = _position[1];

  var enemy = null;
  var lastEnemyPosition = null;
  var timeOnScreen = 0;
  var velocity = [0, -speed];

  var calculatePosition = function calculatePosition(ctx) {
    lastEnemyPosition = enemy.getPosition();
    var desired = maths.calculateTrajectory([x, y], lastEnemyPosition, speed);
    var steering = Math.min(1, ctx.timeSinceLastFrame / 180);
    velocity[0] += (desired[0] - velocity[0]) * steering;
    velocity[1] += (desired[1] - velocity[1]) * steering;
    x += velocity[0] * ctx.timeSinceLastFrame / 1000;
    y += velocity[1] * ctx.timeSinceLastFrame / 1000;
  };

  var instance = {
    isProjectile: true,
    teleport: function teleport(position, direction) {
      x = position[0];
      y = position[1];
      var currentSpeed = Math.hypot.apply(Math, _toConsumableArray(velocity));
      velocity = direction.map(function (component) {
        return component * currentSpeed;
      });
    },
    accelerate: function accelerate(ax, ay, dt) {
      velocity[0] += ax * dt / 1000;
      velocity[1] += ay * dt / 1000;
    },
    hit: function hit(sprite) {
      return instance.removeFromLayer();
    },
    getDamage: function getDamage() {
      return damage;
    },
    getExtent: function getExtent() {
      return {
        x: x,
        y: y,
        radius: radius
      };
    },
    render: function render(ctx) {
      if (!enemy || enemy.destroyed) {
        enemy = maths.getNearest([x, y], ctx.enemies.all());

        if (!enemy) {
          return instance.removeFromLayer();
        }
      }

      timeOnScreen += ctx.timeSinceLastFrame;

      if (timeOnScreen > 2400) {
        return instance.removeFromLayer();
      }

      calculatePosition(ctx);
      drawProjectile(ctx.buffer, {
        x: x,
        y: y,
        velocity: velocity,
        radius: radius,
        colour: '#aaff79',
        length: 38
      });
    }
  };
  return instance;
};

},{"../engine/maths":12,"./projectile":36}],31:[function(require,module,exports){
"use strict";

var et = require('eventthing');

module.exports = function () {
  var score = 0;
  var wave = {
    number: 1,
    total: 9
  };
  var complete = false;
  var round = 1;
  var transitioning = false;
  var announcement = '';
  var announcementAge = 2000;

  var announce = function announce(text) {
    announcement = text;
    announcementAge = 0;
  };

  var weapons = ['Blaster'];
  et.on('enemy_destroyed', function (points) {
    score += points;
  });
  et.on('wave', function (value) {
    wave = value;
    announce('WAVE ' + value.number + ' / ' + value.total);
  });
  et.on('boss_arrival', function (name) {
    return announce('WARNING: ' + (name || 'HIVE QUEEN'));
  });
  et.on('boss_phase', function (phase) {
    return announce('BOSS ENRAGED - PHASE ' + phase);
  });
  et.on('round_complete', function () {
    transitioning = true;
  });
  et.on('round_started', function (value) {
    round = value.number;
    transitioning = false;
    wave = {
      number: 1,
      total: 9
    };
    announce('ROUND ' + round + ' - INTO THE RIFT');
  });
  et.on('level_complete', function () {
    complete = true;
  });
  et.on('upgrade', function (upgrade) {
    if (upgrade.weapon && upgrade.name) weapons.push(upgrade.name);
  });
  return {
    reset: function reset() {
      score = 0;
      wave = {
        number: 1,
        total: 9
      };
      complete = false;
      round = 1;
      transitioning = false;
      announcement = '';
      announcementAge = 2000;
      weapons.splice(0, weapons.length, 'Blaster');
    },
    render: function render(ctx) {
      var buffer = ctx.buffer,
          ship = ctx.ship,
          enemies = ctx.enemies;
      var width = window.innerWidth;
      var height = window.innerHeight;
      buffer.save();
      buffer.font = '14px Orbitron, monospace';
      buffer.fillStyle = '#c7eaff';
      buffer.fillText('SCORE ' + score.toString().padStart(5, '0'), 20, 65);
      buffer.textAlign = 'right';
      buffer.fillText('ROUND ' + round + ' / 2', width - 20, 86);
      buffer.fillText('WAVE ' + wave.number + ' / ' + wave.total, width - 20, 65);
      buffer.textAlign = 'center';
      buffer.fillStyle = '#65e8ff';
      buffer.fillText('SHIELD ' + ship.getShield(), width / 2, 65);
      buffer.fillStyle = 'rgba(8, 15, 32, 0.85)';
      buffer.fillRect(0, height - 62, width, 62);
      buffer.textAlign = 'center';
      buffer.font = '12px Orbitron, monospace';
      buffer.fillStyle = '#65e8ff';
      buffer.fillText(weapons.join('  /  '), width / 2, height - 37, width - 24);
      buffer.fillStyle = '#a4b6cf';
      buffer.fillText('ARROWS: MOVE   |   SPACE: FIRE   |   R: RESTART   |   M: SOUND   |   COLLECT UPGRADES', width / 2, height - 15, width - 24);
      var cannon = ctx.weapons.all().find(function (weapon) {
        return weapon.getHeat;
      });

      if (cannon) {
        buffer.textAlign = 'left';
        buffer.fillStyle = cannon.isOverheated() ? '#ff805f' : '#d994ff';
        buffer.fillText(cannon.isOverheated() ? 'BEAM COOLING' : 'BEAM HEAT', 20, height - 90);
        buffer.fillStyle = '#382439';
        buffer.fillRect(20, height - 82, 130, 5);
        buffer.fillStyle = cannon.isOverheated() ? '#ff805f' : '#d994ff';
        buffer.fillRect(20, height - 82, cannon.getHeat() * 1.3, 5);
        buffer.textAlign = 'center';
      }

      var boss = enemies.all().find(function (enemy) {
        return enemy.isBoss;
      });

      if (boss) {
        var barWidth = Math.min(420, width - 40);
        buffer.fillStyle = '#ff9dd8';
        buffer.fillText(boss.getName() + ' / PHASE ' + boss.getPhase(), width / 2, 88);
        buffer.fillStyle = '#382439';
        buffer.fillRect((width - barWidth) / 2, 98, barWidth, 10);
        buffer.fillStyle = '#ff9dd8';
        buffer.shadowColor = '#ff9dd8';
        buffer.shadowBlur = 10;
        buffer.fillRect((width - barWidth) / 2, 98, barWidth * boss.getHealth() / boss.getMaxHealth(), 10);
        buffer.shadowBlur = 0;
      }

      announcementAge += ctx.timeSinceLastFrame;

      if (announcementAge < 1800 && !ship.isDestroyed()) {
        buffer.save();
        buffer.globalAlpha = Math.min(1, (1800 - announcementAge) / 400);
        buffer.fillStyle = boss ? '#ff9dd8' : '#65e8ff';
        buffer.shadowColor = buffer.fillStyle;
        buffer.shadowBlur = 18;
        buffer.font = '24px Orbitron, monospace';
        buffer.fillText(announcement, width / 2, height * 0.32, width - 40);
        buffer.restore();
      }

      if (transitioning || ship.isDestroyed() || complete && enemies.all().length === 0) {
        buffer.fillStyle = 'rgba(8, 15, 32, 0.8)';
        buffer.fillRect(0, 0, width, height);
        buffer.fillStyle = ship.isDestroyed() ? '#ff9266' : '#65e8ff';
        buffer.font = '32px Orbitron, monospace';
        buffer.fillText(ship.isDestroyed() ? 'SHIP DESTROYED' : transitioning ? 'ROUND 1 CLEARED' : 'SECTORS CLEARED', width / 2, height / 2 - 25, width - 30);
        buffer.font = '16px Orbitron, monospace';
        buffer.fillStyle = '#ffffff';
        buffer.fillText(transitioning && !ship.isDestroyed() ? 'ROUND 2 INCOMING - HEALTH AND UPGRADES RETAINED' : 'SCORE ' + score + '  |  PRESS R TO RESTART', width / 2, height / 2 + 20, width - 30);
      }

      buffer.restore();
    }
  };
};

},{"eventthing":19}],32:[function(require,module,exports){
"use strict";

function _slicedToArray(arr, i) { return _arrayWithHoles(arr) || _iterableToArrayLimit(arr, i) || _nonIterableRest(); }

function _nonIterableRest() { throw new TypeError("Invalid attempt to destructure non-iterable instance"); }

function _iterableToArrayLimit(arr, i) { var _arr = []; var _n = true; var _d = false; var _e = undefined; try { for (var _i = arr[Symbol.iterator](), _s; !(_n = (_s = _i.next()).done); _n = true) { _arr.push(_s.value); if (i && _arr.length === i) break; } } catch (err) { _d = true; _e = err; } finally { try { if (!_n && _i["return"] != null) _i["return"](); } finally { if (_d) throw _e; } } return _arr; }

function _arrayWithHoles(arr) { if (Array.isArray(arr)) return arr; }

var HeatSeeker = require('./heat_seaker');

var et = require('eventthing');

module.exports = function (props) {
  var rate = props.rate,
      offset = props.offset,
      damage = props.damage;

  var _offset = _slicedToArray(offset, 2),
      dx = _offset[0],
      dy = _offset[1];

  var lastFired = 0;
  return {
    fire: function fire(ctx) {
      var elapsedTime = ctx.gameTime - lastFired;

      if (elapsedTime >= rate) {
        lastFired = ctx.gameTime;

        var _ctx$ship$getPosition = ctx.ship.getPosition(),
            _ctx$ship$getPosition2 = _slicedToArray(_ctx$ship$getPosition, 2),
            x = _ctx$ship$getPosition2[0],
            y = _ctx$ship$getPosition2[1];

        et.fire('weapon_fire', {
          kind: 'missile',
          colour: '#aaff79',
          position: [x + dx, y + dy]
        });
        return [HeatSeeker({
          position: [x + dx, y + dy],
          radius: 5,
          damage: damage
        })];
      }
    },
    render: function render() {}
  };
};

},{"./heat_seaker":30,"eventthing":19}],33:[function(require,module,exports){
"use strict";

function _toConsumableArray(arr) { return _arrayWithoutHoles(arr) || _iterableToArray(arr) || _nonIterableSpread(); }

function _nonIterableSpread() { throw new TypeError("Invalid attempt to spread non-iterable instance"); }

function _iterableToArray(iter) { if (Symbol.iterator in Object(iter) || Object.prototype.toString.call(iter) === "[object Arguments]") return Array.from(iter); }

function _arrayWithoutHoles(arr) { if (Array.isArray(arr)) { for (var i = 0, arr2 = new Array(arr.length); i < arr.length; i++) { arr2[i] = arr[i]; } return arr2; } }

module.exports = function (props) {
  var position = props.position,
      colour = props.colour;
  var age = 0;
  var instance = {
    render: function render(ctx) {
      age += ctx.timeSinceLastFrame;
      if (age >= 90) return instance.removeFromLayer();
      var buffer = ctx.buffer;
      var size = 18 * (1 - age / 90);
      buffer.save();
      buffer.translate.apply(buffer, _toConsumableArray(position));
      buffer.globalCompositeOperation = 'lighter';
      buffer.strokeStyle = colour;
      buffer.shadowColor = colour;
      buffer.shadowBlur = 15;
      buffer.lineWidth = 2;
      buffer.beginPath();
      buffer.moveTo(-size, 0);
      buffer.lineTo(size, 0);
      buffer.moveTo(0, -size);
      buffer.lineTo(0, size);
      buffer.stroke();
      buffer.restore();
    }
  };
  return instance;
};

},{}],34:[function(require,module,exports){
"use strict";

function _slicedToArray(arr, i) { return _arrayWithHoles(arr) || _iterableToArrayLimit(arr, i) || _nonIterableRest(); }

function _nonIterableRest() { throw new TypeError("Invalid attempt to destructure non-iterable instance"); }

function _iterableToArrayLimit(arr, i) { var _arr = []; var _n = true; var _d = false; var _e = undefined; try { for (var _i = arr[Symbol.iterator](), _s; !(_n = (_s = _i.next()).done); _n = true) { _arr.push(_s.value); if (i && _arr.length === i) break; } } catch (err) { _d = true; _e = err; } finally { try { if (!_n && _i["return"] != null) _i["return"](); } finally { if (_d) throw _e; } } return _arr; }

function _arrayWithHoles(arr) { if (Array.isArray(arr)) return arr; }

var Bullet = require('./bullet');

var et = require('eventthing');

module.exports = function () {
  var lastFired = -Infinity;
  return {
    fire: function fire(ctx) {
      if (ctx.gameTime - lastFired < 1400) return [];
      lastFired = ctx.gameTime;

      var _ctx$ship$getPosition = ctx.ship.getPosition(),
          _ctx$ship$getPosition2 = _slicedToArray(_ctx$ship$getPosition, 2),
          x = _ctx$ship$getPosition2[0],
          y = _ctx$ship$getPosition2[1];

      et.fire('weapon_fire', {
        kind: 'rotary',
        colour: '#a9c5ff',
        position: [x, y]
      });
      return Array.from({
        length: 12
      }, function (_, i) {
        var angle = i * Math.PI / 6 + ctx.gameTime / 1800;
        return Bullet({
          position: [x + Math.cos(angle) * 28, y + Math.sin(angle) * 28],
          velocity: [Math.cos(angle) * 360, Math.sin(angle) * 360],
          radius: 7,
          damage: 9,
          colour: '#a9c5ff'
        });
      });
    },
    render: function render() {}
  };
};

},{"./bullet":26,"eventthing":19}],35:[function(require,module,exports){
"use strict";

function _toConsumableArray(arr) { return _arrayWithoutHoles(arr) || _iterableToArray(arr) || _nonIterableSpread(); }

function _nonIterableSpread() { throw new TypeError("Invalid attempt to spread non-iterable instance"); }

function _iterableToArray(iter) { if (Symbol.iterator in Object(iter) || Object.prototype.toString.call(iter) === "[object Arguments]") return Array.from(iter); }

function _arrayWithoutHoles(arr) { if (Array.isArray(arr)) { for (var i = 0, arr2 = new Array(arr.length); i < arr.length; i++) { arr2[i] = arr[i]; } return arr2; } }

var et = require('eventthing');

var applyGravity = require('../engine/gravity_field');

var drawWell = require('./well_graphic');

module.exports = function () {
  var props = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {};
  var _props$duration = props.duration,
      duration = _props$duration === void 0 ? 22000 : _props$duration,
      _props$phase = props.phase,
      phase = _props$phase === void 0 ? 0 : _props$phase;
  var age = 0;

  var positions = function positions() {
    var orbit = Math.min(145, window.innerWidth * 0.2, window.innerHeight * 0.18);
    var angle = phase + age / 2600;
    var dx = Math.cos(angle) * orbit;
    var dy = Math.sin(angle) * orbit;
    var x = window.innerWidth * 0.5;
    var y = window.innerHeight * 0.43;
    return {
      black: [x + dx, y + dy],
      white: [x - dx, y - dy]
    };
  };

  var instance = {
    getPositions: positions,
    update: function update(ctx, layers) {
      age += ctx.timeSinceLastFrame;
      if (age >= duration) return instance.removeFromLayer();
      if (age < 1500) return;

      var _positions = positions(),
          black = _positions.black,
          white = _positions.white;

      applyGravity(ctx, layers, {
        position: black,
        radius: 24,
        range: 320,
        strength: 2400,
        enter: function enter(sprite) {
          if (!sprite.teleport || (sprite.portalCooldownUntil || 0) > ctx.gameTime) return false;
          var extent = sprite.getExtent();
          var angle = Math.atan2(white[1] - black[1], white[0] - black[0]);
          var direction = [Math.cos(angle), Math.sin(angle)];
          var distance = 24 + extent.radius + 22;
          var margin = Math.min(extent.radius + 6, window.innerWidth / 2, window.innerHeight / 2);
          var exit = [Math.max(margin, Math.min(window.innerWidth - margin, white[0] + direction[0] * distance)), Math.max(margin, Math.min(window.innerHeight - margin, white[1] + direction[1] * distance))];
          sprite.teleport(exit, direction);
          sprite.portalCooldownUntil = ctx.gameTime + 1400;
          et.fire('portal_transit', {
            position: exit,
            colour: '#c7ffff'
          });
          return true;
        }
      });
      applyGravity(ctx, layers, {
        position: white,
        radius: 24,
        range: 300,
        strength: 2000,
        repel: true
      });
    },
    render: function render(ctx) {
      var _ctx$buffer, _ctx$buffer2;

      var _positions2 = positions(),
          black = _positions2.black,
          white = _positions2.white;

      ctx.buffer.save();
      ctx.buffer.globalAlpha = 0.2;
      ctx.buffer.strokeStyle = '#b7caff';
      ctx.buffer.lineWidth = 1;
      ctx.buffer.beginPath();

      (_ctx$buffer = ctx.buffer).moveTo.apply(_ctx$buffer, _toConsumableArray(black));

      (_ctx$buffer2 = ctx.buffer).lineTo.apply(_ctx$buffer2, _toConsumableArray(white));

      ctx.buffer.stroke();
      ctx.buffer.restore();
      drawWell(ctx, {
        position: black,
        age: age,
        remaining: duration - age,
        label: 'IN / BLACK HOLE'
      });
      drawWell(ctx, {
        position: white,
        age: age,
        remaining: duration - age,
        white: true,
        label: 'OUT / WHITE HOLE'
      });
    }
  };
  return instance;
};

},{"../engine/gravity_field":6,"./well_graphic":46,"eventthing":19}],36:[function(require,module,exports){
"use strict";

function _slicedToArray(arr, i) { return _arrayWithHoles(arr) || _iterableToArrayLimit(arr, i) || _nonIterableRest(); }

function _nonIterableRest() { throw new TypeError("Invalid attempt to destructure non-iterable instance"); }

function _iterableToArrayLimit(arr, i) { var _arr = []; var _n = true; var _d = false; var _e = undefined; try { for (var _i = arr[Symbol.iterator](), _s; !(_n = (_s = _i.next()).done); _n = true) { _arr.push(_s.value); if (i && _arr.length === i) break; } } catch (err) { _d = true; _e = err; } finally { try { if (!_n && _i["return"] != null) _i["return"](); } finally { if (_d) throw _e; } } return _arr; }

function _arrayWithHoles(arr) { if (Array.isArray(arr)) return arr; }

module.exports = function (buffer, props) {
  var x = props.x,
      y = props.y,
      velocity = props.velocity,
      radius = props.radius,
      colour = props.colour,
      length = props.length,
      end = props.end,
      _props$beam = props.beam,
      beam = _props$beam === void 0 ? false : _props$beam;
  var angle = Math.atan2(velocity[1], velocity[0]);

  var _ref = end || [x - Math.cos(angle) * length, y - Math.sin(angle) * length],
      _ref2 = _slicedToArray(_ref, 2),
      tailX = _ref2[0],
      tailY = _ref2[1];

  buffer.save();
  if (beam) buffer.globalCompositeOperation = 'lighter';
  buffer.lineCap = 'round';
  buffer.shadowColor = colour;
  buffer.shadowBlur = 14;
  var trail = buffer.createLinearGradient(tailX, tailY, x, y);
  trail.addColorStop(0, beam ? colour : 'transparent');
  trail.addColorStop(1, colour);
  buffer.strokeStyle = trail;
  buffer.lineWidth = radius * 2;
  buffer.beginPath();
  buffer.moveTo(tailX, tailY);
  buffer.lineTo(x, y);
  buffer.stroke();

  if (!beam) {
    buffer.fillStyle = colour;
    buffer.beginPath();
    buffer.arc(x, y, radius, 0, Math.PI * 2);
    buffer.fill();
  }

  buffer.shadowBlur = 0;

  if (beam) {
    buffer.strokeStyle = '#ffffff';
    buffer.lineWidth = radius * 0.7;
    buffer.beginPath();
    buffer.moveTo(tailX, tailY);
    buffer.lineTo(x, y);
    buffer.stroke();
  } else {
    buffer.fillStyle = '#ffffff';
    buffer.beginPath();
    buffer.arc(x, y, Math.max(2, radius * 0.45), 0, Math.PI * 2);
    buffer.fill();
  }

  buffer.restore();
};

},{}],37:[function(require,module,exports){
"use strict";

function _slicedToArray(arr, i) { return _arrayWithHoles(arr) || _iterableToArrayLimit(arr, i) || _nonIterableRest(); }

function _nonIterableRest() { throw new TypeError("Invalid attempt to destructure non-iterable instance"); }

function _iterableToArrayLimit(arr, i) { var _arr = []; var _n = true; var _d = false; var _e = undefined; try { for (var _i = arr[Symbol.iterator](), _s; !(_n = (_s = _i.next()).done); _n = true) { _arr.push(_s.value); if (i && _arr.length === i) break; } } catch (err) { _d = true; _e = err; } finally { try { if (!_n && _i["return"] != null) _i["return"](); } finally { if (_d) throw _e; } } return _arr; }

function _arrayWithHoles(arr) { if (Array.isArray(arr)) return arr; }

var et = require('eventthing');

var Bullet = require('./bullet');

var Drone = require('./drone_enemy');

var maths = require('../engine/maths');

var GravityMotion = require('../engine/gravity_motion');

module.exports = function (props) {
  var position = props.position,
      _props$carrier = props.carrier,
      carrier = _props$carrier === void 0 ? false : _props$carrier;

  var _position = _slicedToArray(position, 2),
      x = _position[0],
      y = _position[1];

  var energy = carrier ? 220 : 75;
  var age = 0;
  var lastFired = 0;
  var lastBrood = 0;
  var broods = 0;
  var dashDirection = null;
  var gravity = GravityMotion();
  var colour = carrier ? '#ffa6be' : '#ffb765';
  var instance = {
    accelerate: gravity.accelerate,
    teleport: function teleport(position) {
      x = position[0];
      y = position[1];
      dashDirection = null;
      gravity.reset();
    },
    getPosition: function getPosition() {
      return [x, y];
    },
    getExtent: function getExtent() {
      return {
        x: x,
        y: y,
        radius: carrier ? 38 : 22
      };
    },
    getDamage: function getDamage() {
      return carrier ? 25 : 18;
    },
    hit: function hit(sprite) {
      if (instance.destroyed) return;
      energy -= sprite.getDamage();
      et.fire('explosion', {
        position: [x, y],
        velocity: [0, 25],
        colour: colour,
        size: energy <= 0 ? carrier ? 2 : 1 : 0.1
      });

      if (energy <= 0) {
        instance.removeFromLayer();
        et.fire('enemy_destroyed', carrier ? 400 : 250);
      }
    },
    fire: function fire(ctx) {
      if (age < 1800 || y < 0) return [];

      if (carrier && broods < 3 && age - lastBrood >= 6000) {
        lastBrood = age;
        broods++;

        for (var _i2 = 0, _arr2 = [-26, 26]; _i2 < _arr2.length; _i2++) {
          var dx = _arr2[_i2];
          et.fire('create_enemy', Drone({
            position: [x + dx, y + 35]
          }));
        }
      }

      if (ctx.gameTime - lastFired < (carrier ? 2400 : 1500)) return [];
      lastFired = ctx.gameTime;
      var heading = maths.calculateHeading([x, y], ctx.ship.getPosition());
      return (carrier ? [-0.24, 0, 0.24] : [0]).map(function (offset) {
        return Bullet({
          position: [x, y],
          velocity: [Math.cos(heading + offset) * 250, Math.sin(heading + offset) * 250],
          radius: 6,
          colour: '#ff9266',
          damage: 7
        });
      });
    },
    render: function render(ctx) {
      age += ctx.timeSinceLastFrame;
      var dashing = !carrier && age % 3000 > 2300;
      var warning = !carrier && age % 3000 > 1800 && !dashing;
      var target = carrier ? [x, window.innerHeight * 0.25] : ctx.ship.getPosition();
      var distance = Math.hypot(target[0] - x, target[1] - y);
      if (dashing && !dashDirection) dashDirection = maths.calculateTrajectory([x, y], target, 1);
      if (!dashing) dashDirection = null;
      var heading = dashing ? dashDirection.map(function (component) {
        return component * 260 * ctx.timeSinceLastFrame / 1000;
      }) : maths.calculateTrajectory([x, y], target, Math.min(distance, (carrier ? 28 : 65) * ctx.timeSinceLastFrame / 1000));
      var pull = gravity.step(ctx.timeSinceLastFrame);
      x += heading[0] + pull[0];
      y += heading[1] + pull[1];
      var buffer = ctx.buffer;
      buffer.save();
      buffer.translate(x, y);
      buffer.rotate(carrier ? Math.sin(age / 500) * 0.12 : (dashDirection ? Math.atan2(dashDirection[1], dashDirection[0]) : maths.calculateHeading([x, y], target)) - Math.PI / 2);
      buffer.shadowColor = colour;
      buffer.shadowBlur = dashing ? 26 : 14;
      buffer.strokeStyle = colour;
      buffer.fillStyle = '#382d45';
      buffer.lineWidth = 2;
      buffer.beginPath();

      if (carrier) {
        buffer.moveTo(-42, -26);
        buffer.lineTo(42, -26);
        buffer.lineTo(48, 20);
        buffer.lineTo(0, 44);
        buffer.lineTo(-48, 20);
      } else {
        buffer.moveTo(0, 34);
        buffer.lineTo(-25, -24);
        buffer.lineTo(0, -9);
        buffer.lineTo(25, -24);
      }

      buffer.closePath();
      buffer.fill();
      buffer.stroke();
      buffer.fillStyle = '#fff0bf';
      buffer.beginPath();
      buffer.arc(0, 0, carrier ? 12 : 6, 0, Math.PI * 2);
      buffer.fill();

      if (warning) {
        buffer.globalAlpha = 0.3 + Math.sin(age / 35) * 0.15;
        buffer.beginPath();
        buffer.moveTo(0, 38);
        buffer.lineTo(0, 180);
        buffer.stroke();
      }

      if (carrier) {
        buffer.fillStyle = '#ffa6be';

        for (var _i3 = 0, _arr3 = [-28, 28]; _i3 < _arr3.length; _i3++) {
          var dx = _arr3[_i3];
          buffer.fillRect(dx - 6, 4, 12, 18);
        }
      }

      buffer.restore();
    }
  };
  return instance;
};

},{"../engine/gravity_motion":7,"../engine/maths":12,"./bullet":26,"./drone_enemy":27,"eventthing":19}],38:[function(require,module,exports){
"use strict";

function _slicedToArray(arr, i) { return _arrayWithHoles(arr) || _iterableToArrayLimit(arr, i) || _nonIterableRest(); }

function _nonIterableRest() { throw new TypeError("Invalid attempt to destructure non-iterable instance"); }

function _iterableToArrayLimit(arr, i) { var _arr = []; var _n = true; var _d = false; var _e = undefined; try { for (var _i = arr[Symbol.iterator](), _s; !(_n = (_s = _i.next()).done); _n = true) { _arr.push(_s.value); if (i && _arr.length === i) break; } } catch (err) { _d = true; _e = err; } finally { try { if (!_n && _i["return"] != null) _i["return"](); } finally { if (_d) throw _e; } } return _arr; }

function _arrayWithHoles(arr) { if (Array.isArray(arr)) return arr; }

var Bullet = require('./bullet');

var et = require('eventthing');

module.exports = function () {
  var lastFired = -Infinity;
  return {
    fire: function fire(ctx) {
      if (ctx.gameTime - lastFired < 80) return [];
      lastFired = ctx.gameTime;

      var _ctx$ship$getPosition = ctx.ship.getPosition(),
          _ctx$ship$getPosition2 = _slicedToArray(_ctx$ship$getPosition, 2),
          x = _ctx$ship$getPosition2[0],
          y = _ctx$ship$getPosition2[1];

      return [0, Math.PI].map(function (offset) {
        var angle = ctx.gameTime / 600 + offset;
        var position = [x + Math.cos(angle) * 32, y + Math.sin(angle) * 32];
        et.fire('weapon_fire', {
          kind: 'rotary',
          colour: '#87ffe4',
          position: position
        });
        return Bullet({
          position: position,
          velocity: [Math.cos(angle) * 420, Math.sin(angle) * 420],
          damage: 2,
          radius: 4,
          colour: '#87ffe4'
        });
      });
    },
    render: function render(ctx) {
      if (ctx.ship.isDestroyed()) return;

      var _ctx$ship$getPosition3 = ctx.ship.getPosition(),
          _ctx$ship$getPosition4 = _slicedToArray(_ctx$ship$getPosition3, 2),
          x = _ctx$ship$getPosition4[0],
          y = _ctx$ship$getPosition4[1];

      var buffer = ctx.buffer;
      buffer.save();
      buffer.translate(x, y);
      buffer.rotate(ctx.gameTime / 600);
      buffer.strokeStyle = '#87ffe4';
      buffer.lineWidth = 2;
      buffer.beginPath();
      buffer.arc(0, 0, 32, 0, Math.PI * 2);
      buffer.stroke();
      buffer.fillStyle = '#d9fff6';
      buffer.fillRect(-38, -3, 10, 6);
      buffer.fillRect(28, -3, 10, 6);
      buffer.restore();
    }
  };
};

},{"./bullet":26,"eventthing":19}],39:[function(require,module,exports){
"use strict";

function _slicedToArray(arr, i) { return _arrayWithHoles(arr) || _iterableToArrayLimit(arr, i) || _nonIterableRest(); }

function _nonIterableRest() { throw new TypeError("Invalid attempt to destructure non-iterable instance"); }

function _iterableToArrayLimit(arr, i) { var _arr = []; var _n = true; var _d = false; var _e = undefined; try { for (var _i = arr[Symbol.iterator](), _s; !(_n = (_s = _i.next()).done); _n = true) { _arr.push(_s.value); if (i && _arr.length === i) break; } } catch (err) { _d = true; _e = err; } finally { try { if (!_n && _i["return"] != null) _i["return"](); } finally { if (_d) throw _e; } } return _arr; }

function _arrayWithHoles(arr) { if (Array.isArray(arr)) return arr; }

var et = require('eventthing');

var GravityMotion = require('../engine/gravity_motion');

var width = 25;
var height = 25;
var colour = 'rgb(152, 195, 121)';
var img = new Image();
img.src = 'svg/interceptor.svg';

module.exports = function (props) {
  var x = props.x,
      y = props.y,
      maxSpeed = props.maxSpeed,
      energy = props.energy;
  var destroyed = false;
  var invulnerability = 0;
  var shield = 0;
  var keys = {};
  var gravity = GravityMotion();
  et.on('keychange', function (newKeys) {
    return keys = newKeys;
  });
  et.on('upgrade', function (upgrade) {
    if (upgrade.speedup) maxSpeed = Math.min(500, maxSpeed + upgrade.speedup);
    if (upgrade.shield) shield = Math.min(100, shield + upgrade.shield);

    if (upgrade.energy) {
      energy += upgrade.energy;
      energy = Math.max(0, Math.min(100, energy));
    }
  });

  var calculatePosition = function calculatePosition(dt) {
    var _gravity$step = gravity.step(dt),
        _gravity$step2 = _slicedToArray(_gravity$step, 2),
        pullX = _gravity$step2[0],
        pullY = _gravity$step2[1];

    var newX = x + pullX;
    var newY = y + pullY;
    if (keys.ArrowLeft && !keys.ArrowRight) newX -= maxSpeed * dt / 1000;
    if (keys.ArrowRight && !keys.ArrowLeft) newX += maxSpeed * dt / 1000;
    if (keys.ArrowUp && !keys.ArrowDown) newY -= maxSpeed * dt / 1000;
    if (keys.ArrowDown && !keys.ArrowUp) newY += maxSpeed * dt / 1000;
    x = Math.max(30, Math.min(window.innerWidth - 30, newX));
    y = Math.max(30, Math.min(window.innerHeight - 30, newY));
  };

  var instance = {
    isPlayer: true,
    teleportVersion: 0,
    accelerate: gravity.accelerate,
    teleport: function teleport(position) {
      x = Math.max(30, Math.min(window.innerWidth - 30, position[0]));
      y = Math.max(30, Math.min(window.innerHeight - 30, position[1]));
      gravity.reset();
      invulnerability = Math.max(invulnerability, 900);
      instance.teleportVersion++;
    },
    hit: function hit(sprite) {
      if (destroyed || invulnerability > 0 || sprite.getDamage() === 0) return;
      var damage = sprite.getDamage();
      var absorbed = Math.min(shield, damage);
      shield -= absorbed;
      energy -= damage - absorbed;
      invulnerability = 650;
      et.fire('ship_hit');
      energy = Math.max(0, Math.min(100, energy));

      if (energy <= 0) {
        et.fire('explosion', {
          position: [x, y],
          velocity: [0, 0],
          colour: colour,
          size: 5
        });
        et.fire('death');
        destroyed = true;
      } else {
        et.fire('explosion', {
          position: [x, y],
          velocity: [0, 0],
          colour: colour,
          size: 0.1
        });
      }
    },
    getHealth: function getHealth() {
      return energy;
    },
    getShield: function getShield() {
      return shield;
    },
    isDestroyed: function isDestroyed() {
      return destroyed;
    },
    getDamage: function getDamage() {
      return 100000;
    },
    getExtent: function getExtent() {
      return {
        x: x,
        y: y,
        radius: 25
      };
    },
    recreate: function recreate() {
      destroyed = false;
      energy = 100;
      shield = 0;
      invulnerability = 0;
      maxSpeed = props.maxSpeed;
      x = window.innerWidth * 0.5;
      y = window.innerHeight * 0.7;
      keys = {};
      gravity.reset();
      instance.teleportVersion = 0;
      instance.portalCooldownUntil = 0;
    },
    getPosition: function getPosition() {
      return [x, y];
    },
    getDimensions: function getDimensions() {
      return [width, height];
    },
    render: function render(ctx) {
      if (destroyed) return;
      invulnerability = Math.max(0, invulnerability - ctx.timeSinceLastFrame);
      calculatePosition(ctx.timeSinceLastFrame);
      ctx.buffer.save();
      ctx.buffer.shadowColor = '#65e8ff';
      ctx.buffer.shadowBlur = 18;

      if (shield > 0) {
        ctx.buffer.strokeStyle = '#65e8ff';
        ctx.buffer.lineWidth = 3;
        ctx.buffer.beginPath();
        ctx.buffer.arc(x, y, 35, -Math.PI / 2, -Math.PI / 2 + Math.PI * 2 * shield / 100);
        ctx.buffer.stroke();
      }

      ctx.buffer.fillStyle = '#497dff';
      ctx.buffer.beginPath();
      ctx.buffer.moveTo(x - 12, y + 22);
      ctx.buffer.lineTo(x, y + 68 + Math.sin(ctx.gameTime / 45) * 12);
      ctx.buffer.lineTo(x + 12, y + 22);
      ctx.buffer.fill();
      ctx.buffer.fillStyle = '#c7f8ff';
      ctx.buffer.beginPath();
      ctx.buffer.moveTo(x - 5, y + 22);
      ctx.buffer.lineTo(x, y + 48 + Math.sin(ctx.gameTime / 45) * 6);
      ctx.buffer.lineTo(x + 5, y + 22);
      ctx.buffer.fill();
      if (invulnerability > 0) ctx.buffer.globalAlpha = 0.5 + Math.sin(ctx.gameTime / 40) * 0.25;
      ctx.buffer.drawImage(img, x - 32, y - 36, 64, 72);
      ctx.buffer.restore();
    }
  };
  return instance;
};

},{"../engine/gravity_motion":7,"eventthing":19}],40:[function(require,module,exports){
"use strict";

function _slicedToArray(arr, i) { return _arrayWithHoles(arr) || _iterableToArrayLimit(arr, i) || _nonIterableRest(); }

function _nonIterableRest() { throw new TypeError("Invalid attempt to destructure non-iterable instance"); }

function _iterableToArrayLimit(arr, i) { var _arr = []; var _n = true; var _d = false; var _e = undefined; try { for (var _i = arr[Symbol.iterator](), _s; !(_n = (_s = _i.next()).done); _n = true) { _arr.push(_s.value); if (i && _arr.length === i) break; } } catch (err) { _d = true; _e = err; } finally { try { if (!_n && _i["return"] != null) _i["return"](); } finally { if (_d) throw _e; } } return _arr; }

function _arrayWithHoles(arr) { if (Array.isArray(arr)) return arr; }

var Bullet = require('./bullet');

var et = require('eventthing');

module.exports = function (props) {
  var rate = props.rate,
      velocity = props.velocity,
      offset = props.offset,
      damage = props.damage,
      _props$colour = props.colour,
      colour = _props$colour === void 0 ? '#65e8ff' : _props$colour,
      _props$radius = props.radius,
      radius = _props$radius === void 0 ? 6 : _props$radius,
      _props$barrels = props.barrels,
      barrels = _props$barrels === void 0 ? [{
    angle: 0,
    offset: offset
  }] : _props$barrels,
      _props$pierce = props.pierce,
      pierce = _props$pierce === void 0 ? 1 : _props$pierce,
      trailLength = props.trailLength;
  var lastFired = 0;
  return {
    fire: function fire(ctx) {
      var elapsedTime = ctx.gameTime - lastFired;

      if (elapsedTime >= rate) {
        lastFired = ctx.gameTime;

        var _ctx$ship$getPosition = ctx.ship.getPosition(),
            _ctx$ship$getPosition2 = _slicedToArray(_ctx$ship$getPosition, 2),
            x = _ctx$ship$getPosition2[0],
            y = _ctx$ship$getPosition2[1];

        return barrels.map(function (barrel) {
          var _barrel$offset = _slicedToArray(barrel.offset, 2),
              dx = _barrel$offset[0],
              dy = _barrel$offset[1];

          var angle = barrel.angle;
          et.fire('weapon_fire', {
            kind: 'pulse',
            colour: colour,
            position: [x + dx, y + dy]
          });
          return Bullet({
            position: [x + dx, y + dy],
            velocity: [velocity[0] * Math.cos(angle) - velocity[1] * Math.sin(angle), velocity[0] * Math.sin(angle) + velocity[1] * Math.cos(angle)],
            radius: radius,
            colour: colour,
            damage: damage,
            pierce: pierce,
            trailLength: trailLength
          });
        });
      }
    },
    render: function render() {}
  };
};

},{"./bullet":26,"eventthing":19}],41:[function(require,module,exports){
"use strict";

function _slicedToArray(arr, i) { return _arrayWithHoles(arr) || _iterableToArrayLimit(arr, i) || _nonIterableRest(); }

function _nonIterableRest() { throw new TypeError("Invalid attempt to destructure non-iterable instance"); }

function _iterableToArrayLimit(arr, i) { var _arr = []; var _n = true; var _d = false; var _e = undefined; try { for (var _i = arr[Symbol.iterator](), _s; !(_n = (_s = _i.next()).done); _n = true) { _arr.push(_s.value); if (i && _arr.length === i) break; } } catch (err) { _d = true; _e = err; } finally { try { if (!_n && _i["return"] != null) _i["return"](); } finally { if (_d) throw _e; } } return _arr; }

function _arrayWithHoles(arr) { if (Array.isArray(arr)) return arr; }

var Bullet = require('./bullet');

var et = require('eventthing');

var maths = require('../engine/maths');

var GravityMotion = require('../engine/gravity_motion');

var img = new Image();
img.src = 'svg/noun_tarantula_734014.svg';

module.exports = function (props) {
  var position = props.position,
      speed = props.speed,
      radius = props.radius,
      colour = props.colour,
      energy = props.energy,
      rate = props.rate;

  var _position = _slicedToArray(position, 2),
      x = _position[0],
      y = _position[1];

  var lastFired = 0;
  var targetLocation = [window.innerWidth / 2, window.innerHeight / 2];

  var calculatePosition = function calculatePosition(ctx) {
    var newHeading = maths.calculateTrajectory([x, y], targetLocation, speed * ctx.timeSinceLastFrame / 1000);
    x += newHeading[0];
    y += newHeading[1];
  };

  var spriteTime = 0;
  var gravity = GravityMotion();
  var instance = {
    accelerate: gravity.accelerate,
    teleport: function teleport(position) {
      x = position[0];
      y = position[1];
      gravity.reset();
    },
    hit: function hit(sprite) {
      if (instance.destroyed) return;
      energy -= sprite.getDamage();
      var explosionHeading = maths.calculateTrajectory([x, y], targetLocation, speed);

      if (energy <= 0) {
        et.fire('explosion', {
          position: [x, y],
          velocity: explosionHeading,
          colour: colour,
          size: 1
        });
        instance.removeFromLayer();
        et.fire('enemy_destroyed', 150);
        return;
      }

      et.fire('explosion', {
        position: [x, y],
        velocity: explosionHeading,
        colour: colour,
        size: 0.1
      });
    },
    getDamage: function getDamage() {
      return 20;
    },
    getPosition: function getPosition() {
      return [x, y];
    },
    getExtent: function getExtent() {
      return {
        x: x,
        y: y,
        radius: radius
      };
    },
    fire: function fire(ctx) {
      if (spriteTime < 3000) return;
      var elapsedTime = ctx.gameTime - lastFired;

      if (elapsedTime >= rate) {
        lastFired = ctx.gameTime;
        var shipPosition = ctx.ship.getPosition();
        return [Bullet({
          position: [x, y],
          velocity: maths.calculateTrajectory([x, y], shipPosition, 200),
          radius: 7,
          colour: '#ff789e',
          damage: 5
        })];
      }
    },
    render: function render(ctx) {
      spriteTime += ctx.timeSinceLastFrame;
      if (spriteTime < 3000) calculatePosition(ctx);

      var _gravity$step = gravity.step(ctx.timeSinceLastFrame),
          _gravity$step2 = _slicedToArray(_gravity$step, 2),
          pullX = _gravity$step2[0],
          pullY = _gravity$step2[1];

      x += pullX;
      y += pullY;
      var angle = maths.calculateHeading([x, y], [window.innerWidth / 2, window.innerHeight / 2]) + Math.PI * 0.5;
      ctx.buffer.save();
      ctx.buffer.translate(x, y);
      ctx.buffer.shadowColor = colour;
      ctx.buffer.shadowBlur = 15;
      ctx.buffer.rotate(angle);
      ctx.buffer.drawImage(img, -40, -40, 80, 80);
      ctx.buffer.restore();
    }
  };
  return instance;
};

},{"../engine/gravity_motion":7,"../engine/maths":12,"./bullet":26,"eventthing":19}],42:[function(require,module,exports){
"use strict";

var twopi = 2 * Math.PI;

module.exports = function (props) {
  var x = props.x,
      y = props.y,
      speed = props.speed,
      radius = props.radius,
      colour = props.colour;

  var calculatePosition = function calculatePosition(dt) {
    y += speed * dt / 1000;

    if (y - radius * 2 > window.innerHeight) {
      y = radius * -2;
      x = Math.random() * window.innerWidth;
    }
  };

  return {
    render: function render(ctx) {
      calculatePosition(ctx.timeSinceLastFrame);
      ctx.buffer.save();
      ctx.buffer.globalAlpha = 0.65 + Math.sin(ctx.gameTime / 700 + x) * 0.3;
      ctx.buffer.fillStyle = colour;

      if (radius > 2) {
        ctx.buffer.strokeStyle = colour;
        ctx.buffer.lineWidth = 1;
        ctx.buffer.beginPath();
        ctx.buffer.moveTo(x, y);
        ctx.buffer.lineTo(x, y - radius * 4);
        ctx.buffer.stroke();
      }

      ctx.buffer.beginPath();
      ctx.buffer.arc(x, y, radius, 0, twopi);
      ctx.buffer.fill();
      ctx.buffer.restore();
    }
  };
};

},{}],43:[function(require,module,exports){
"use strict";

function _slicedToArray(arr, i) { return _arrayWithHoles(arr) || _iterableToArrayLimit(arr, i) || _nonIterableRest(); }

function _nonIterableRest() { throw new TypeError("Invalid attempt to destructure non-iterable instance"); }

function _iterableToArrayLimit(arr, i) { var _arr = []; var _n = true; var _d = false; var _e = undefined; try { for (var _i = arr[Symbol.iterator](), _s; !(_n = (_s = _i.next()).done); _n = true) { _arr.push(_s.value); if (i && _arr.length === i) break; } } catch (err) { _d = true; _e = err; } finally { try { if (!_n && _i["return"] != null) _i["return"](); } finally { if (_d) throw _e; } } return _arr; }

function _arrayWithHoles(arr) { if (Array.isArray(arr)) return arr; }

module.exports = function (props) {
  var position = props.position,
      text = props.text,
      colour = props.colour,
      velocity = props.velocity;

  var _position = _slicedToArray(position, 2),
      x = _position[0],
      y = _position[1];

  var life = 0;

  var calculatePosition = function calculatePosition(ctx) {
    x += velocity[0] * ctx.timeSinceLastFrame / 1000;
    y += velocity[1] * ctx.timeSinceLastFrame / 1000;
  };

  var instance = {
    render: function render(ctx) {
      calculatePosition(ctx);
      ctx.buffer.fillStyle = colour;
      ctx.buffer.fillText(text, x, y);
      life += ctx.timeSinceLastFrame;

      if (life > 2000) {
        instance.removeFromLayer();
      }
    }
  };
  return instance;
};

},{}],44:[function(require,module,exports){
"use strict";

function _slicedToArray(arr, i) { return _arrayWithHoles(arr) || _iterableToArrayLimit(arr, i) || _nonIterableRest(); }

function _nonIterableRest() { throw new TypeError("Invalid attempt to destructure non-iterable instance"); }

function _iterableToArrayLimit(arr, i) { var _arr = []; var _n = true; var _d = false; var _e = undefined; try { for (var _i = arr[Symbol.iterator](), _s; !(_n = (_s = _i.next()).done); _n = true) { _arr.push(_s.value); if (i && _arr.length === i) break; } } catch (err) { _d = true; _e = err; } finally { try { if (!_n && _i["return"] != null) _i["return"](); } finally { if (_d) throw _e; } } return _arr; }

function _arrayWithHoles(arr) { if (Array.isArray(arr)) return arr; }

var twopi = 2 * Math.PI;

var et = require('eventthing');

var maths = require('../engine/maths');

var GravityMotion = require('../engine/gravity_motion');

module.exports = function (props) {
  var position = props.position,
      speed = props.speed,
      radius = props.radius,
      colour = props.colour,
      upgrade = props.upgrade;

  var _position = _slicedToArray(position, 2),
      x = _position[0],
      y = _position[1];

  var targetX = x;
  var life = 0;
  var gravity = GravityMotion();

  var calculatePosition = function calculatePosition(ctx) {
    var target = [targetX, window.innerHeight * 0.6];
    var distance = Math.hypot(target[0] - x, target[1] - y);
    var newHeading = maths.calculateTrajectory([x, y], target, Math.min(distance, speed * ctx.timeSinceLastFrame / 1000));

    var _gravity$step = gravity.step(ctx.timeSinceLastFrame),
        _gravity$step2 = _slicedToArray(_gravity$step, 2),
        pullX = _gravity$step2[0],
        pullY = _gravity$step2[1];

    x += newHeading[0] + pullX;
    y += newHeading[1] + pullY;
  };

  var instance = {
    accelerate: gravity.accelerate,
    teleport: function teleport(position) {
      x = position[0];
      y = position[1];
      targetX = x;
      gravity.reset();
    },
    hit: function hit(sprite) {
      et.fire('upgrade', upgrade), instance.removeFromLayer();
      et.fire('display_text', {
        position: [x + 15, y + 15],
        colour: colour,
        text: upgrade.text,
        velocity: [0, -10]
      });
    },
    getDamage: function getDamage() {
      return 0;
    },
    getExtent: function getExtent() {
      return {
        x: x,
        y: y,
        radius: radius
      };
    },
    render: function render(ctx) {
      calculatePosition(ctx);
      ctx.buffer.save();
      ctx.buffer.shadowColor = colour;
      ctx.buffer.shadowBlur = 20;
      ctx.buffer.strokeStyle = '#fff';
      ctx.buffer.lineWidth = 2;

      for (var i = 0; i < 3; i++) {
        var angle = life / 350 + i * twopi / 3;
        ctx.buffer.beginPath();
        ctx.buffer.arc(x, y, radius + 8, angle, angle + 0.9);
        ctx.buffer.stroke();
      }

      if (Math.floor(life / 500) % 5 === 0) {
        ctx.buffer.lineWidth = 1;
        ctx.buffer.beginPath();
        ctx.buffer.arc(x, y, life % 500 / 20, 0, twopi);
        ctx.buffer.stroke();
      }

      ctx.buffer.fillStyle = colour;
      ctx.buffer.lineWidth = 1;
      ctx.buffer.beginPath();
      ctx.buffer.arc(x, y, radius, 0, twopi);
      ctx.buffer.stroke();
      ctx.buffer.fill();
      ctx.buffer.font = '13px Orbitron, monospace';
      ctx.buffer.textAlign = 'center';
      ctx.buffer.fillText(upgrade.text, x, y - 36);
      ctx.buffer.restore();
      life += ctx.timeSinceLastFrame;
    }
  };
  return instance;
};

},{"../engine/gravity_motion":7,"../engine/maths":12,"eventthing":19}],45:[function(require,module,exports){
"use strict";

function _slicedToArray(arr, i) { return _arrayWithHoles(arr) || _iterableToArrayLimit(arr, i) || _nonIterableRest(); }

function _nonIterableRest() { throw new TypeError("Invalid attempt to destructure non-iterable instance"); }

function _iterableToArrayLimit(arr, i) { var _arr = []; var _n = true; var _d = false; var _e = undefined; try { for (var _i = arr[Symbol.iterator](), _s; !(_n = (_s = _i.next()).done); _n = true) { _arr.push(_s.value); if (i && _arr.length === i) break; } } catch (err) { _d = true; _e = err; } finally { try { if (!_n && _i["return"] != null) _i["return"](); } finally { if (_d) throw _e; } } return _arr; }

function _arrayWithHoles(arr) { if (Array.isArray(arr)) return arr; }

var et = require('eventthing');

var Bullet = require('./bullet');

var maths = require('../engine/maths');

var GravityMotion = require('../engine/gravity_motion');

var img = new Image();
img.src = 'svg/noun_potter wasp_720014.svg';

module.exports = function (props) {
  var position = props.position,
      _props$phase = props.phase,
      phase = _props$phase === void 0 ? 0 : _props$phase;

  var _position = _slicedToArray(position, 2),
      startX = _position[0],
      startY = _position[1];

  var x = startX;
  var y = startY;
  var age = 0;
  var lastFired = 0;
  var energy = 45;
  var gravity = GravityMotion();
  var driftX = 0;
  var driftY = 0;
  var pathX = x;
  var pathY = y;
  var instance = {
    accelerate: gravity.accelerate,
    teleport: function teleport(position) {
      x = position[0];
      y = position[1];
      driftX = x - pathX;
      driftY = y - pathY;
      gravity.reset();
    },
    getPosition: function getPosition() {
      return [x, y];
    },
    getExtent: function getExtent() {
      return {
        x: x,
        y: y,
        radius: 24
      };
    },
    getDamage: function getDamage() {
      return 20;
    },
    hit: function hit(sprite) {
      if (instance.destroyed) return;
      energy -= sprite.getDamage();
      et.fire('explosion', {
        position: [x, y],
        velocity: [0, 60],
        colour: '#ffd36a',
        size: energy <= 0 ? 1 : 0.1
      });

      if (energy <= 0) {
        instance.removeFromLayer();
        et.fire('enemy_destroyed', 200);
      }
    },
    fire: function fire(ctx) {
      if (age < 1500 || y < 0 || ctx.gameTime - lastFired < 1700) return [];
      lastFired = ctx.gameTime;
      var heading = maths.calculateHeading([x, y], ctx.ship.getPosition());
      return [-0.12, 0.12].map(function (angle) {
        return Bullet({
          position: [x, y],
          velocity: [Math.cos(heading + angle) * 240, Math.sin(heading + angle) * 240],
          radius: 6,
          colour: '#ffbc59',
          damage: 7
        });
      });
    },
    render: function render(ctx) {
      age += ctx.timeSinceLastFrame;

      var _gravity$step = gravity.step(ctx.timeSinceLastFrame),
          _gravity$step2 = _slicedToArray(_gravity$step, 2),
          pullX = _gravity$step2[0],
          pullY = _gravity$step2[1];

      driftX += pullX;
      driftY += pullY;
      var amplitude = Math.min(100, window.innerWidth * 0.15);
      pathX = startX + Math.sin(age / 420 + phase) * amplitude;
      pathY = startY + age * 0.07;
      x = Math.max(30, Math.min(window.innerWidth - 30, pathX + driftX));
      y = pathY + driftY;
      if (y > window.innerHeight + 60) return instance.removeFromLayer();
      var buffer = ctx.buffer;
      buffer.save();
      buffer.translate(x, y);
      buffer.rotate(Math.sin(age / 420 + phase) * 0.4 + Math.PI);
      buffer.shadowColor = '#ffd36a';
      buffer.shadowBlur = 18;
      buffer.drawImage(img, -38, -38, 76, 76);
      buffer.restore();
    }
  };
  return instance;
};

},{"../engine/gravity_motion":7,"../engine/maths":12,"./bullet":26,"eventthing":19}],46:[function(require,module,exports){
"use strict";

function _slicedToArray(arr, i) { return _arrayWithHoles(arr) || _iterableToArrayLimit(arr, i) || _nonIterableRest(); }

function _nonIterableRest() { throw new TypeError("Invalid attempt to destructure non-iterable instance"); }

function _iterableToArrayLimit(arr, i) { var _arr = []; var _n = true; var _d = false; var _e = undefined; try { for (var _i = arr[Symbol.iterator](), _s; !(_n = (_s = _i.next()).done); _n = true) { _arr.push(_s.value); if (i && _arr.length === i) break; } } catch (err) { _d = true; _e = err; } finally { try { if (!_n && _i["return"] != null) _i["return"](); } finally { if (_d) throw _e; } } return _arr; }

function _arrayWithHoles(arr) { if (Array.isArray(arr)) return arr; }

module.exports = function (ctx, props) {
  var _props$position = _slicedToArray(props.position, 2),
      x = _props$position[0],
      y = _props$position[1],
      age = props.age,
      remaining = props.remaining,
      _props$white = props.white,
      white = _props$white === void 0 ? false : _props$white,
      label = props.label;

  var buffer = ctx.buffer;
  buffer.save();
  buffer.translate(x, y);
  buffer.globalAlpha = Math.max(0, Math.min(1, remaining / 1000));
  buffer.strokeStyle = white ? '#c7ffff' : '#be9bff';
  buffer.shadowColor = white ? '#8fffff' : '#ac7aff';
  buffer.shadowBlur = white ? 24 : 16;
  buffer.lineWidth = 2;

  for (var i = 0; i < 4; i++) {
    var orbit = 36 + i * 10;
    var angle = age / (240 + i * 60) * (white ? -1 : 1) + i;
    buffer.beginPath();
    buffer.ellipse(0, 0, orbit, orbit * 0.5, angle, 0, Math.PI * 1.6);
    buffer.stroke();
  }

  buffer.beginPath();
  buffer.arc(0, 0, 24, 0, Math.PI * 2);
  buffer.fillStyle = white ? '#efffff' : age < 1500 ? '#33234c' : '#02020a';
  buffer.fill();
  buffer.stroke();
  buffer.shadowBlur = 0;
  buffer.font = '11px Orbitron, monospace';
  buffer.textAlign = 'center';
  buffer.fillStyle = white ? '#dfffff' : '#d9c6ff';
  buffer.fillText(age < 1500 ? 'GRAVITY WELL FORMING' : label, 0, -75);
  buffer.restore();
};

},{}]},{},[14]);
